# Salmón Ahumado Fuyén — Página Web

Notas de proyecto (para Joaquín y para Claude). Este archivo se mantiene actualizado
a medida que avanza el desarrollo — no es documentación estática.

## Reglas de trabajo con Claude

- **Nunca hacer `git commit` ni `git push` sin preguntar primero**, aunque el pedido
  del cambio haya sido explícito y claro. Mostrar los cambios hechos y esperar
  confirmación antes de comitear.

## Qué es esto

Tienda online **privada** (no indexada, sin acceso público) para vender salmón
ahumado artesanal, mientras la formalización legal del negocio está pendiente.

- Acceso solo vía link con token secreto (`/acceso/:token`) + `X-Robots-Tag: noindex`.
- Pago por transferencia bancaria (sin pasarela de pago).
- Venta por pieza/porción (no por kilo completo): salmón ahumado en pieza pequeña o grande, salmón ahumado desmenuzado y paté de salmón ahumado. ~25 kg disponibles por semana en total.
- Ciclo de producción semanal, entregas los viernes.

## Descripción y funciones principales

Sitio a medida para que Joaquín venda salmón ahumado artesanal por internet sin
depender de un marketplace ni exponer la tienda al público general — solo entra
quien tiene el link con token de acceso. No hay pasarela de pago: el flujo completo
está pensado para transferencia bancaria coordinada por WhatsApp, con el negocio
confirmando el pago a mano.

**Para el cliente:**
- Ve el catálogo (filtros de precio/categoría, orden, búsqueda visual por tarjetas), agrega productos al carrito y ajusta cantidades sin recargar la página.
- Se registra o inicia sesión (Devise, roles cliente/admin) y guarda una o varias direcciones de entrega, con mapa asistido para ubicar la calle.
- En el checkout revisa el resumen de la compra (con el envío ya calculado), elige la fecha de entrega en un calendario que solo permite viernes futuros, y confirma.
- El pedido queda registrado en estado "pendiente de pago"; la pantalla de confirmación tiene un botón para avisarle al negocio por WhatsApp con el detalle completo ya escrito (productos, total, envío, fecha y dirección de entrega).
- Puede ver su perfil e historial de pedidos, o consultar el estado de un pedido puntual por código sin necesidad de cuenta (`/mi-pedido`).

**Para el negocio (panel admin):**
- Ve y avanza el estado de cada pedido (pendiente de pago → pagado → en preparación → despachado → entregado) o lo cancela, confirmando el pago a mano después de revisar la conversación de WhatsApp.
- Descarga un Excel filtrado por una fecha de despacho puntual (un viernes), con todo lo que hay que preparar esa semana y a quién.
- CRUD completo de productos (bloqueado el borrado si el producto ya tiene pedidos asociados, para no perder historial de ventas) y listado/borrado de clientes.

## Stack

- Ruby on Rails 8.1.3 + PostgreSQL
- Devise para autenticación, roles `:cliente` / `:admin`
- Desplegado en Render.com (**filesystem efímero** — por eso las fotos de producto son assets estáticos del repo, ver más abajo)

## Modelos principales

- `User` (Devise, role enum: cliente/admin; nombre/apellido separados; teléfono validado con formato)
- `Direccion` (pertenece a `User` — un cliente puede tener varias, con etiqueta libre tipo "Casa"/"Oficina", comuna restringida a 5 comunas fijas — Lo Barnechea, Las Condes, Vitacura, Providencia, La Reina —, calle, lat/lng opcional y flag `principal`. Nota: "direccion" está registrado como plural irregular en `config/initializers/inflections.rb` porque el inflector de Rails no adivina bien el español)
- `Product` (nombre, descripción, precio, stock, peso_descripcion, activo, `foto_filename` — referencia a un archivo estático en `public/docs/productos/`, `categoria` enum — hoy solo `ahumado`, listo para sumar más)
- `Order` (codigo_pedido autogenerado tipo `FUY-XXXXXX`, estado, total, `envio` — $2.000 o gratis desde $40.000, ver `CostoEnvio` —, notas, `fecha_entrega` — elegida por el cliente en el checkout, siempre un viernes —, `lat`/`lng` de la dirección de entrega, verificadas por el propio servidor contra Nominatim al confirmar el pedido — nunca se confía en coordenadas que mande el cliente, ver `GeocodificadorService`)
- `OrderItem` (cantidad, precio_unitario)
- `AccessToken` (tokens de acceso al sitio)
- `Resena` (pertenece a `User` y `Product` — `calificacion` 1-5, `comentario` opcional; un cliente reseña un producto una sola vez y solo si lo compró)

## Controladores principales

- `AccesoController` (`entrar`, `invalido`, `bienvenida`) — valida el token de la URL y abre la sesión de "sitio visible" (no confundir con el login de usuario).
- `PagesController` (`inicio`, `nosotros`) — páginas estáticas de la home.
- `ProductosController` (`index`, `show`) — catálogo con filtros/orden y ficha de producto.
- `CarritoController` (`show`, `agregar`, `actualizar`, `quitar`) — carrito en sesión, no se persiste en base hasta el checkout.
- `CheckoutController` (`show`, `create`) — datos de envío, fecha de entrega y creación del pedido.
- `PedidosPublicosController` (`nuevo`, `buscar`, `show`) — pantalla de "pedido recibido" y consulta de un pedido por código sin necesidad de cuenta.
- `PerfilController` (`show`, `edit`, `update`) — datos propios e historial de pedidos del cliente logueado.
- `DireccionesController` (`new`, `create`, `edit`, `update`, `destroy`) — CRUD de direcciones del cliente.
- `ResenasController` (`create`) — anidado bajo productos, solo clientes que compraron el producto pueden reseñarlo.
- `Admin::PedidosController` (`index`, `show`, `avanzar_estado`, `cancelar`, `exportar`) — panel de pedidos y export a Excel.
- `Admin::ProductsController` (`index`, `new`, `create`, `edit`, `update`, `destroy`) — CRUD de productos.
- `Admin::UsersController` (`index`, `show`, `destroy`) — listado/borrado de clientes.
- `Admin::BaseController` — controller base del panel admin (exige sesión + rol admin, sin acciones propias, todo el admin hereda de él).
- `ApplicationController` — controller base de todo el sitio (valida el token de acceso en cada request, desactiva flashes automáticos de Devise, agrega `X-Robots-Tag: noindex`, sin acciones propias).

## Estado actual (ya implementado)

- [x] Autenticación Devise con roles cliente/admin
- [x] Modelos Product, Order, OrderItem + códigos de pedido auto-generados
- [x] Catálogo y carrito de compras
- [x] Checkout con pago por transferencia bancaria
- [x] Panel de administración: **Pedidos** (ver, avanzar estado, cancelar), **Productos** (CRUD completo, con bloqueo de borrado si tiene pedidos asociados), **Usuarios** (listado de clientes + detalle con su historial de pedidos, solo lectura)
- [x] Acceso protegido por token secreto + noindex
- [x] Paleta de color definida (2026-07-08): azul marino/océano (`--navy` #081f2e, `--ocean` #145577) + dorado apagado de detalle (`--gold` #b8934f) sobre fondos frío-claros (`--foam`)
- [x] Perfil de cliente (`/perfil`, solo clientes — el admin no tiene perfil, gestiona usuarios desde el panel): ver y editar datos propios, ver historial de pedidos; ícono de perfil en la nav
- [x] Flashes automáticos de Devise desactivados (login/logout/registro ya no muestran notificación; los flashes propios del negocio siguen funcionando — **ojo:** esto también silencia el mensaje de "email o contraseña inválidos" en un login fallido, es intencional según lo pedido)
- [x] Íconos de carrito, perfil y mostrar/ocultar contraseña como SVG de línea (sin emojis)
- [x] Fotos de producto como **asset estático versionado en el repo** (2026-07-30): se descartó Active Storage + Cloudinary (no convencía depender de un servicio externo). Ahora `Product#foto_filename` guarda solo el nombre de archivo; la imagen vive en `public/docs/productos/<archivo>` y Rails la sirve directo como estática (sin ruta ni controlador propio, `/docs/productos/<archivo>`). Se eliminó Active Storage por completo (gem `image_processing`, tablas `active_storage_*`, config de servicio). **Trade-off aceptado:** ya no se sube la foto desde el panel admin — hay que copiar el archivo a `public/docs/productos/`, commitear y deployar, y luego escribir el nombre exacto del archivo en el campo "Archivo de foto" del admin (`/admin/productos`). Sube la complejidad de actualizar una foto, pero elimina la dependencia de un backend externo y el problema del filesystem efímero de raíz.
- [x] Direcciones múltiples por cliente con nombre propio (Casa/Oficina/etc.), selección de comuna asistida con mapa (Leaflet + geocodificación OpenStreetMap Nominatim, sin API key) que se actualiza mientras el cliente escribe la calle. CRUD completo desde el perfil; siempre hay una dirección "principal" (se reasigna sola si se borra la que estaba marcada).
- [x] Mensajes de validación de todo el sitio en español (gem `rails-i18n` + `default_locale: :es`), incluida validación de formato de teléfono.
- [x] Panel admin: "Admin"/"Administrador" ya no se resaltan con color/pill — mismo formato que el resto del texto del nav. "(Principal)" en las direcciones tampoco se resalta, es texto gris discreto.
- [x] Fix alineación botones "Editar"/"Eliminar" en tabla de Productos (el `<form>` que genera `button_to` rompía el `flex` de la fila — se resuelve con `display: contents` en `.acciones-fila form` / `.direccion-card-acciones form`, aplica a cualquier fila que mezcle `link_to` + `button_to`).
- [x] Fix toggle de mostrar/ocultar contraseña: dependía del atributo HTML `hidden` en `<svg>`, poco confiable entre navegadores — ahora usa `style.display` explícito desde Stimulus.
- [x] Mapa de direcciones (Leaflet + Nominatim, sin API key) confirmado como solución definitiva (2026-07-29) — funciona bien en registro/perfil/checkout. **Se descarta la migración a Mapbox** que estaba planeada antes; no vale la pena el costo de integrarla para la mejora marginal que daría.
- [x] **Modal "producto añadido al carrito"** (2026-07-30). Overlay oscuro + modal centrado con foto/nombre/precio/cantidad agregada a la izquierda, resumen del carrito (artículos, subtotal, envío "A calcular", total) a la derecha, botones "Seguir comprando" (cierra, `btn-secondary`) y "Ver carrito" (navega, `btn-ocean`, usa `--ocean` #145577 existente). Cierra con el botón X, "Seguir comprando", click en el overlay o tecla Escape (`app/javascript/controllers/modal_carrito_controller.js`). Se dispara **solo desde la página de detalle de producto** (ver ítem siguiente) — desde el catálogo, agregar al carrito no abre modal, ver más abajo.
- [x] **Rediseño del catálogo "Nuestros productos"** (2026-07-30). Layout de 2 columnas: sidebar de filtros (precio desde/hasta, categoría) a la izquierda, selector de orden (Posición/Precio asc/Precio desc/Nombre, ancho `flex: 1` para llenar la fila) + grilla a la derecha. Todo dentro de un solo `<form method="get">` que apunta a un `turbo_frame_tag "productos-grid"` (`data-turbo-frame` en el form, no dentro del frame) — así un cambio de orden nunca pisa los filtros activos ni viceversa, y nada recarga la página completa. Auto-submit en radios/select vía `app/javascript/controllers/filtros_controller.js`; el rango de precio tiene botón "Aplicar filtros" explícito. Página fluida a todo el ancho (`content_for :fluid` + `.catalogo-page` a 1400px, no el `.container` de 960px del resto del sitio). Tarjeta: fondo blanco + borde + sombra suave tipo "cuadro" independiente, foto cuadrada con padding parejo alrededor (no pegada al borde), nombre en una sola línea (ellipsis si es muy largo, mayúsculas vía CSS) y precio centrado sin negrita debajo. El botón "Agregar al carrito"/stepper `− cantidad +` está superpuesto sobre la foto y solo aparece en `:hover`/`:focus-within` (desktop); en mobile/touch (`@media (hover: none)`) queda siempre visible. Al agregar desde la tarjeta **no se abre el modal**: el botón se reemplaza in-place por el stepper (Turbo Stream sobre `#producto-accion-<id>`) que pega contra `CarritoController#actualizar` (PATCH) y refleja la cantidad real del carrito; el badge del carrito en la nav (`app/views/shared/_nav_carrito.html.erb`) se actualiza en el mismo response. Página de detalle (`/productos/:id`): foto grande con el mismo marco tipo tarjeta que el catálogo, nombre, precio, descripción, selector de cantidad local (Stimulus, sin pegarle al server hasta enviar el form) y botón "Agregar al carrito" full-width que sí dispara el modal. `CarritoController#agregar` distingue el origen con `params[:contexto]` ("catalogo" vs "detalle"). Nota de idioma: "categoria" se agregó a `config/initializers/inflections.rb` como plural irregular (mismo problema que ya tenía "direccion"), si no `Product.categorias` no existía como método.
- [x] **Sticky footer global** (2026-07-30). En páginas con poco contenido quedaba un espacio de fondo visible debajo del footer. `body` ahora es `display: flex; flex-direction: column; min-height: 100vh` y el contenido principal del layout se envolvió en `<main class="site-main">` con `flex: 1 0 auto`, así el footer siempre queda pegado al borde inferior de la ventana. Aplicado una sola vez en el layout compartido, afecta a todo el sitio.
- [x] **Fix "Agregar al carrito" poco fiable** (2026-08-05). Los 3 forms de agregar/actualizar carrito (`productos/show.html.erb`, `productos/_accion_carrito.html.erb`) tenían `data: { turbo_stream: true }`, que fuerza el `Accept` header a aceptar **solo** `turbo-stream` — si el server alguna vez no podía responder turbo-stream (token CSRF vencido, sesión expirada, etc.) Turbo simplemente no hacía nada, sin error visible, y el fallback `format.html` ya codeado en el controller quedaba inalcanzable. Turbo Drive ya manda turbo-stream en el `Accept` por defecto en un submit normal, así que se sacó ese atributo en los 3 forms — restaura el fallback sin perder el comportamiento normal. De paso, el selector de cantidad de la página de detalle ahora también clampea en vivo mientras se escribe (antes solo los botones +/- lo hacían), para que un valor fuera de stock no bloquee el submit silenciosamente.
- [x] **Centrado de los botones +/- del stepper** (2026-08-05). `.cantidad-selector` (detalle de producto) y `.stepper-btn` (círculo +/-) no centraban bien su contenido; ahora usan flexbox.
- [x] **Catálogo: limpieza y filtros** (2026-08-05). Se quitó el encabezado de texto ("Nuestros productos — Piezas elaboradas artesanalmente..."). El selector "Ordenar por" se movió de la grilla a la columna de filtros. El filtro de precio pasó de dos inputs numéricos libres (desde/hasta) a rangos predeterminados tipo radio button: Hasta $4.990, $4.990–$9.990, $9.990–$14.990, $14.990–$19.990, Más de $19.990 (`ProductosController::RANGOS_PRECIO`).
- [x] **Admin: eliminar usuario** (2026-08-05). Botón "Eliminar" en `/admin/users` (listado y detalle), con confirmación. Bloqueado si el cliente tiene pedidos asociados (mismo criterio que ya existía para no poder borrar un producto con pedidos — se prioriza no perder historial de ventas).
- [x] **Carrito rediseñado en 2 columnas** (2026-08-05). Columna izquierda: lista de productos con foto, nombre, peso, precio unitario, stepper +/- y botón de quitar (ícono papelera) — todo actualiza in-place vía Turbo Stream, incluido el caso de vaciar el carrito por completo. Columna derecha: tarjeta "Resumen de compra" (costo de productos, envío "A calcular", total) con botón "Continuar con el pago →" que lleva al checkout.
- [x] **Checkout en dos pasos** (2026-08-05). Antes "Confirmar pedido" iba directo a los datos de transferencia. Ahora hay un paso intermedio: formulario de datos de envío (nombre, apellido, teléfono, calle, comuna, notas) **prellenado con los datos del perfil si hay sesión** (nombre/apellido/teléfono del usuario, comuna/calle de su dirección principal) pero editable ahí mismo antes de confirmar. `Order` ahora guarda una copia de estos datos al momento del pedido (migración `AddDatosEnvioToOrders`: `nombre_contacto`, `apellido_contacto`, `telefono_contacto`, `direccion_calle`, `direccion_comuna`) — así si el cliente cambia su dirección de perfil después, el pedido ya despachado conserva la dirección real a la que se entregó. El admin ve estos datos en el detalle del pedido (con fallback a la dirección del perfil para pedidos creados antes de este cambio). "Finalizar compra →" crea el pedido y lleva a la pantalla de transferencia bancaria de siempre, que ahora tiene botones de copiar (ícono junto a cada dato + botón "Copiar todos los datos") vía `app/javascript/controllers/copiar_controller.js` (usa `navigator.clipboard`, funciona en localhost/HTTPS).
- [x] **Fix bug estructural: `<form>` anidados en el catálogo** (2026-08-05). Causa raíz de dos síntomas reportados como "inconsistentes entre productos": (1) tarjetas donde la foto se encogía dejando espacio en blanco con el selector de cantidad desalineado, y (2) el click en "Agregar al carrito" a veces no agregaba nada / recargaba la grilla en vez de mostrar el stepper. El form de filtros (`form_with url: productos_path...`) envolvía **toda** la grilla de productos, y cada tarjeta (`productos/_accion_carrito.html.erb`) renderiza su propio `<form>` para agregar/actualizar el carrito — HTML no permite anidar `<form>`, así que el navegador descartaba silenciosamente el `<form>` interno de cada tarjeta y sus botones terminaban perteneciendo al `<form>` de filtros (GET a `/productos`). El síntoma variaba según qué tan bien tolerara cada navegador ese HTML inválido — de ahí que pareciera "algunos productos sí, otros no". Fix: el form de filtros ahora envuelve **solo** la columna de filtros (`app/views/productos/index.html.erb`); la grilla de productos (con los forms de cada tarjeta) quedó como hermano, fuera de ese form. Verificado con Chromium headless (Playwright) en desktop y mobile — mismo comportamiento en todas las tarjetas.
- [x] **Fix "Content missing" al continuar con el pago** (2026-08-05). Los links "Continuar con el pago", "Seguir comprando" y "Ver productos" del carrito vivían dentro de un `turbo_frame_tag "carrito-content"` (usado para las actualizaciones in-place de cantidad/quitar). Turbo intenta cargar la respuesta de esos links **dentro** del frame por defecto; como `/checkout` y `/productos` no tienen un frame con ese id, Turbo mostraba "Content missing". Se agregó `data: { turbo_frame: "_top" }` a esos 3 links para que naveguen la página completa.
- [x] **Fix `db/seeds.rb` desactualizado — no corría / no mostraba fotos** (2026-08-10). Dos bugs encadenados, mismo origen: el seed no se había actualizado tras migraciones posteriores. (1) Seguía asignando `u.comuna=`/`u.direccion=` directo en `User`, atributos que `MigrateUserAddressesToDirecciones` (2026-07-08) movió a un modelo `Direccion` aparte — el seed abortaba con `NoMethodError` a mitad de camino, dejando la base sin productos ni cliente de prueba. Fix: la dirección del cliente semilla ahora se crea vía `cliente.direcciones.find_or_create_by!(etiqueta: "Casa", comuna:, calle:)`. (2) Los 2 productos de semilla nunca seteaban `foto_filename`, aunque los archivos sí existen en `public/docs/productos/` — por eso el catálogo no mostraba fotos pese a que la solución de assets estáticos (2026-07-30) funcionaba bien. Fix: se agregó `p.update!(foto_filename: "salmon-pieza-*.jpg")` a cada producto de seed. Verificado corriendo `bin/rails db:seed` limpio y probando login + catálogo con curl.
- [x] **Banner/hero en el catálogo + catálogo más ancho** (2026-08-05). Nuevo banner entre el nav y los filtros (`.catalogo-hero`): imagen de fondo con degradé navy encima (mismo `--navy` del resto del sitio, para que se sienta parte del sitio y no un elemento genérico), breadcrumb "Inicio › Nuestros productos", título "NUESTROS PRODUCTOS" y bajada "Salmón ahumado artesanal del sur de Chile" — mismo lenguaje visual que el `.hero` de la home (letter-spacing grande, dorado de acento). **La imagen de fondo es un placeholder** (una copia de la foto de producto ya existente, en `public/docs/banners/catalogo-hero.jpg`) — cuando haya una foto real del salmón/ahumador/proceso artesanal, reemplázala en esa misma ruta (mismo mecanismo que las fotos de producto: archivo estático, sin subir por el admin). Tamaño ideal: ancha, mínimo ~1920×600px, buen contraste en el tercio central-superior porque ahí va el texto — el degradé oscuro ya ayuda a que cualquier foto quede legible. De paso, el contenedor del catálogo pasó de 1400px a 1600px de ancho máximo, y la grilla de productos ahora muestra hasta 5 columnas en pantallas grandes (antes 4) — con los 2 productos de prueba actuales todavía va a quedar espacio vacío a la derecha de las tarjetas, eso es normal y va a dejar de notarse a medida que se agreguen más productos al catálogo, no es algo que el CSS pueda "rellenar" sin estirar las tarjetas de forma rara.

- [x] **Confirmación de pedido vía WhatsApp + export a Excel para el despacho** (2026-08-12, mensaje de WhatsApp ampliado 2026-08-17). Objetivo: no automatizar el pago (sigue siendo transferencia manual, coordinada por WhatsApp) pero sí que cada pedido quede fácil de ver y armar para el despacho semanal, sin que Joaquín tenga que tocar la base de datos a mano.
  - **Flujo del cliente**: al confirmar el checkout, el pedido se crea en la base de datos (`pendiente_pago`) igual que antes, pero ahora la pantalla de "Pedido recibido" pone como **acción principal** un botón "Confirmar pedido por WhatsApp" (verde, arriba de todo) que abre un `wa.me` con el mensaje ya escrito: código de pedido, lista de productos con cantidad y subtotal, total y dirección de entrega. Ese mensaje es lo que el negocio ve en el chat para saber qué preparar y a quién pasarle los datos de la cuenta — los datos bancarios en sí **no se mandan por WhatsApp automáticamente**, los tipea el negocio a mano en la conversación. El mensaje lo arma `app/services/whatsapp_order_message_service.rb` (`WhatsappOrderMessageService`), separado del controller/vista para no repetir la lógica. La tabla de datos bancarios que ya existía en esa misma pantalla se mantiene más abajo, como respaldo por si el cliente ya los tiene, pero dejó de ser la acción principal.
  - **Número de WhatsApp por variable de entorno**: `config/banco.yml` (clave `whatsapp`) ahora lee `ENV["WHATSAPP_BUSINESS_NUMBER"]` con el número de prueba como fallback en desarrollo — hay que setear esa variable en Render con el número real del negocio antes de ir a producción.
  - **Confirmación de pago**: sigue siendo 100% manual desde `/admin/pedidos` (sin cambios) — el admin ve la conversación de WhatsApp y avanza el estado del pedido a mano.
  - **Export a Excel**: nuevo botón "Descargar Excel de despacho" arriba de la lista de `/admin/pedidos`, con un campo de fecha que viene precargado con el próximo viernes (`Date.current.next_occurring(:friday)`) pero el admin puede cambiarlo. Genera y descarga (`send_data`, no se guarda en disco — Render tiene filesystem efímero) un `.xlsx` con **todos los pedidos no cancelados** (no solo los pagados — así el negocio ve de un vistazo la semana completa, con una columna "Estado" para distinguir qué está pagado y qué no), sin importar cuándo se crearon — no hay un campo de "fecha de despacho" guardado por pedido, la fecha que elige el admin solo se usa para nombrar el archivo. Columnas: código, cliente, teléfono, dirección, instrucciones de entrega, productos y cantidades (con el peso/descripción de cada producto), total, estado. Nombre de archivo y de hoja tipo `Pedido_14_de_agosto.xlsx` (meses en español a mano en el servicio, sin depender de locale). Gem usada: `caxlsx` (agregada al Gemfile). Lógica completa en `app/services/pedidos_excel_export_service.rb`.
  - Verificado de punta a punta simulando con `curl`/`rails runner` (login como cliente → agregar al carrito → checkout → pantalla de WhatsApp con el link correcto y mensaje completo; login como admin → botón de export → descarga de un `.xlsx` válido con los pedidos esperados) porque el entorno no tenía Playwright disponible para probarlo en un browser real (probado de nuevo el 2026-08-17, mismo motivo).

- [x] **Fecha de entrega elegida por el cliente + Excel filtrado por esa fecha** (2026-08-17). Antes la "fecha de entrega" era solo una estimación calculada al vuelo (`created_at` + próximo viernes) y nunca se guardaba; el Excel de despacho traía **todos** los pedidos sin importar la fecha, usando la fecha elegida por el admin únicamente para nombrar el archivo. Ahora:
  - **Checkout**: nuevo campo obligatorio "Fecha de entrega" (`app/views/checkout/show.html.erb`), un `<select>` con los próximos 8 viernes disponibles (`Order.viernes_disponibles`). El pedido se guarda con esa fecha en la nueva columna `orders.fecha_entrega` (migración `AddFechaEntregaToOrders`, con backfill de los pedidos existentes usando el criterio antiguo).
  - **No se puede pedir para "hoy mismo"**: si hoy es viernes, la fecha mínima seleccionable es el viernes siguiente (7 días después), no hoy — `Date#next_occurring(:friday)` de ActiveSupport ya tiene ese comportamiento (nunca devuelve la fecha de partida, aunque ya sea viernes), así que `Order.proximo_viernes_habil` lo hereda gratis. Validado en el modelo (`Order`): `fecha_entrega` debe ser viernes y no puede ser anterior al próximo viernes hábil (esta segunda regla solo aplica `on: :create`, para no romper pedidos antiguos cuando el admin avanza su estado días después).
  - **Export a Excel ahora filtra de verdad por fecha**: `PedidosExcelExportService` pasó de exportar todos los pedidos no cancelados a exportar solo los que tienen `fecha_entrega` igual a la fecha elegida por el admin — así "descargar el Excel del viernes 21" trae solo esos pedidos, no la lista completa. Se agregó una columna "Entrega" a la tabla de `/admin/pedidos` para ver de un vistazo a qué viernes corresponde cada pedido.
  - El mensaje de WhatsApp (`WhatsappOrderMessageService`) ahora también incluye la fecha de entrega real del pedido.
  - Verificado con `curl`/`rails runner`: selector muestra viernes correctos desde un lunes de prueba, un viernes ya pasado y una fecha que no es viernes son rechazados con el mensaje de error esperado, y el export a dos fechas de despacho distintas devuelve conjuntos de pedidos distintos y correctos.

- [x] **Calendario visual para elegir fecha de entrega/despacho (solo viernes seleccionables)** (2026-08-17). El `<select>` con lista de viernes del punto anterior se reemplazó por un calendario desplegable de verdad: todos los días que no son viernes aparecen en rojo y no se pueden hacer click (`#c0392b`, mismo rojo de error que usa el resto del sitio); los viernes válidos se ven en azul navy y son clickeables. Componente nuevo y reutilizable, sin librería externa — `app/javascript/controllers/viernes_calendario_controller.js` (Stimulus) + partial `app/views/shared/_selector_viernes.html.erb`. Se usa en dos lugares:
  - **Checkout** (`fecha_entrega`): además de "no viernes → rojo", los viernes anteriores al próximo viernes hábil (la regla de "no se puede pedir para hoy mismo") se muestran deshabilitados en gris (no rojo, para no confundir "día equivocado" con "día válido pero muy pronto").
  - **Export de Excel en `/admin/pedidos`** (`fecha_despacho`): mismo calendario pero sin restricción de mínimo — el admin puede elegir cualquier viernes, pasado o futuro, para volver a descargar el Excel de una semana ya despachada.
  - Verificado que el checkout y el panel admin renderizan el calendario sin errores de servidor, con el `min` correcto en cada caso (`curl`), la lógica de clasificación de días (viernes válido / viernes muy pronto / no-viernes) probada de forma aislada en Node contra fechas reales, y sintaxis del controller JS chequeada. **Confirmado visualmente en un browser real por Joaquín (2026-08-17)**: el click en un viernes lo selecciona bien y los días en rojo no responden al click — también confirmado el botón de WhatsApp y el export de Excel.

- [x] **Costo de envío: $2.000 fijo, gratis desde $40.000** (2026-08-17). Antes el envío no tenía costo — el carrito y el checkout mostraban "Despacho: Viernes" (el día) pero no un monto. Política centralizada en `app/models/costo_envio.rb` (`CostoEnvio::MONTO = 2_000`, `CostoEnvio::GRATIS_DESDE = 40_000`, sobre el subtotal de productos, sin contar el envío mismo). Nueva columna `orders.envio` (migración `AddEnvioToOrders`, default `0` — los pedidos históricos no cobraron envío porque la función no existía, así que dejarlos en 0 es correcto, no se les hizo backfill retroactivo). Visible en **todos los lugares donde se muestra el total de una compra**:
  - Carrito (`carrito/_resumen.html.erb`) y modal "producto añadido" (`carrito/_modal.html.erb`): fila "Envío" con el monto o "Gratis" en verde, más una nota "Envío gratis en compras desde $40.000" que solo aparece si todavía no se alcanza el umbral.
  - Checkout (`checkout/show.html.erb`): misma fila de envío en la tabla resumen antes de "Total a transferir".
  - Pantalla "mi pedido" y detalle del pedido en el admin (`pedidos/_items.html.erb`, reutilizado en ambas): el pie de la tabla de productos ahora tiene tres filas — Subtotal productos, Envío, Total — en vez de solo "Total". El admin además ve "Envío" en la tarjeta resumen de `/admin/pedidos/:id`.
  - Mensaje de WhatsApp (`WhatsappOrderMessageService`): agrega "Subtotal productos" y "Envío" (monto o "Gratis") antes de la línea de "Total".
  - Excel de despacho (`PedidosExcelExportService`): dos columnas nuevas, "Subtotal productos" y "Envío", junto a "Total".
  - `Order#calcular_total` (usado por `db/seeds.rb`) ahora calcula y guarda `envio` además de `total`, así que los pedidos de semilla también quedan con el envío correcto.
  - Verificado con `curl`/`rails runner` de punta a punta con dos pedidos reales: uno de $4.500 (bajo el umbral) cobrando $2.000 de envío con total $6.500, y otro de $40.500 (sobre el umbral) con envío $0 y total $40.500 — confirmado consistente en carrito, checkout, pantalla de pedido, link de WhatsApp, detalle admin y Excel exportado (revisando las celdas numéricas del `.xlsx` directamente, no solo el texto).

- [x] **Calendario de viernes: cuadrados rojo/verde** (2026-08-17). El calendario del punto anterior distinguía "no es viernes" (texto rojo) de "viernes muy pronto" (texto gris) con solo color de texto. Ahora ambos casos no disponibles se ven como **cuadrado rojo sólido** (mismo rojo que el badge "Cancelado" del sitio, `#f8d7da`/`#842029`) y los viernes disponibles como **cuadrado verde sólido** (mismo verde que los badges "Entregado"/"En preparación", `#d1e7dd`/`#0a3622`) — cada día es un cuadrado (`aspect-ratio: 1/1`) en vez de solo un número con color de texto. Se simplificó a un esquema binario (disponible/no disponible) a pedido explícito, en vez de la distinción de tres colores anterior.

- [x] **Email de la empresa actualizado a `ventasfuyen@gmail.com`** (2026-08-17). Antes era `contacto@fuyen.cl` (fallback de desarrollo en `config/contacto.yml`, clave `BUSINESS_EMAIL`). Se usa en un solo lugar del sitio: el footer ("Reclamos:", `app/views/layouts/application.html.erb`). Verificado que el footer ya muestra el nuevo email.

- [x] **Checkout: selector de direcciones guardadas** (2026-08-17). Antes el checkout prellenaba calle/comuna solo con la dirección principal del cliente, sin forma de elegir otra guardada salvo borrando y tipeando a mano. Ahora, si el cliente logueado tiene direcciones guardadas, aparece un bloque "Dirección de entrega" arriba de los campos de calle/comuna con una opción por cada dirección (etiqueta, "(Principal)" si corresponde, calle y comuna) — al elegir una se rellenan esos dos campos (`app/javascript/controllers/direccion_selector_controller.js`), que siguen editables por si el cliente quiere ajustar algo puntual antes de confirmar. No aparece para invitados sin sesión ni para clientes sin direcciones guardadas (sigue funcionando igual que antes, con los campos vacíos o prellenados desde el perfil). Verificado con `curl`/`rails runner`: un cliente con 2 direcciones ve ambas opciones con la principal pre-marcada y los campos prellenados correctamente; el checkout de invitado no muestra el bloque.

- [x] **Suite de tests con 98.86% de cobertura + 2 bugs reales encontrados y corregidos** (2026-08-17). El proyecto no tenía tests reales (solo scaffolds vacíos y fixtures rotas tipo `MyString`/`MyText` que violaban validaciones y uniqueness). Se armó una suite completa:
  - **SimpleCov** configurado en `test/test_helper.rb` con `minimum_coverage 90` (el proceso de test falla si baja de ahí). Reporte HTML en `coverage/index.html` (gitignoreado, se regenera en cada corrida).
  - **Fixtures reemplazadas por factories**: `Order`/`Direccion` tienen callbacks y validaciones cruzadas que las fixtures se saltan, así que en vez de arreglar los `.yml` se armaron métodos (`crear_cliente`, `crear_admin`, `crear_producto`, `crear_direccion`, `crear_pedido`, `crear_token_acceso`) en `test_helper.rb`, más dos helpers para integration tests (`entrar_con_token!`, `iniciar_sesion!` — login real vía POST, sin mockear Devise).
  - **118 tests, 362 assertions, 98.86% de cobertura de línea**: tests de modelo para los 8 modelos (`User`, `Direccion`, `Product`, `Order`, `OrderItem`, `OrderStatusChange`, `AccessToken`, `CostoEnvio`) cubriendo validaciones, callbacks (ej. reasignación de dirección principal) y métodos de negocio; tests de integración que efectivamente "usan la página" end-to-end vía requests reales (no mocks): registro, login/logout, navegar el catálogo con filtros, agregar/actualizar/quitar del carrito (incluyendo las respuestas Turbo Stream), checkout completo con pedido creado y stock descontado, compra de invitado sin cuenta, consulta de pedido por código, perfil y CRUD de direcciones (incluyendo que un cliente no pueda editar la dirección de otro), y en el panel admin: CRUD de productos, listado/borrado de clientes (bloqueado si tienen pedidos), avanzar/cancelar estado de pedidos, y export a Excel filtrado por fecha. Test unitario aparte para `WhatsappOrderMessageService` verificando el texto exacto del mensaje.
  - **Bug real #1 — `CONTACTO` era `nil` en el entorno de test**: `config/contacto.yml` (usado por `Rails.application.config_for(:contacto)`) tenía secciones `development`/`production` pero no `test`, así que el footer (`CONTACTO[:whatsapp]`) reventaba con `NoMethodError` en cualquier test que renderizara una página completa. Se agregó la sección `test:` faltante.
  - **Bug real #2 — el botón de avanzar estado de un pedido en el admin estaba roto**: `Admin::PedidosController#avanzar_estado` llamaba a `estado_label(...)`, un helper de vista (`ApplicationHelper`) que no está disponible directamente en un controller — cualquier click en "Confirmar pago recibido" / "Marcar en preparación" / etc. tiraba `NoMethodError` y un 500. Se corrigió a `helpers.estado_label(...)` (el proxy que expone los view helpers al controller). Este bug no se había detectado en las pruebas manuales anteriores porque nunca se probó ese botón específico por `curl`.
  - **Bonus**: se corrieron Brakeman (análisis de seguridad estático) y Rubocop (estilo) sobre todo el código — Brakeman no encontró ninguna vulnerabilidad; Rubocop solo marcó detalles cosméticos en archivos boilerplate generados por Devise/Rails (comillas, espacios en arrays), nada en el código propio del negocio.
  - El proyecto ya tenía un pipeline de CI en `.github/workflows/ci.yml` (Brakeman, bundler-audit, Rubocop, y `bin/rails test`) que nunca se había ejecutado en serio por falta de tests reales — con esta suite, ahora sí corre algo significativo en cada push/PR.
  - Único hueco de cobertura que quedó (5 líneas, todas del mismo `rescue`): el bloque `rescue ActiveRecord::RecordInvalid` en `CheckoutController#create` — es una red de seguridad para una falla a nivel de base de datos después de pasar la validación normal (ej. una carrera de uniqueness), no alcanzable de forma limpia sin mockear internamente; se dejó sin cubrir a propósito.

- [x] **Buscador de texto en el catálogo** (2026-08-17). Nuevo campo "Buscar" arriba de "Ordenar por" en la columna de filtros de `/productos`, que filtra por `nombre` o `descripcion` (`ILIKE`, insensible a mayúsculas). Usa el mismo botón "Aplicar filtros" que ya existía para el rango de precio (no auto-envía en cada tecla). Verificado con tests de integración y a mano contra el server real.

- [x] **Reseñas de productos con estrellas** (2026-08-17). Nuevo modelo `Resena` (`user`, `product`, `calificacion` 1-5, `comentario` opcional; un cliente solo puede reseñar un producto una vez, y solo si lo compró — validado contra sus `OrderItem` en pedidos no cancelados, sin exigir que ya le haya llegado). En la ficha de cada producto (`/productos/:id`) ahora se ve: el promedio de calificación con estrellas SVG (mismo estilo de ícono de línea del resto del sitio, sin emojis) y la cantidad de reseñas, la lista de reseñas (calificación, nombre del cliente, fecha, comentario si escribió uno), y si el cliente logueado compró el producto y todavía no lo reseñó, un formulario para calificarlo (selector de estrellas 1-5 vía radios + CSS, sin JS) y dejar un comentario opcional. Ruta anidada `resources :productos { resources :resenas, only: [:create] }`. Verificado con tests (modelo + integración: reseña válida, bloqueo de reseña duplicada, bloqueo si nunca compró, pedido cancelado no cuenta como compra) y a mano contra el server real con el cliente de prueba.

- [x] **Fix: no se podían crear/editar productos con precios tipo "$X.990"** (2026-08-17). El campo de precio en `/admin/products` (`app/views/admin/products/_form.html.erb`) tenía `step: 100` en el `number_field` — eso activa la validación nativa del navegador que solo acepta múltiplos de 100, así que un precio como `4990` (no es múltiplo de 100) quedaba bloqueado **del lado del cliente**, sin llegar siquiera a tocar el servidor. Justo en conflicto con la convención de precios del propio negocio (los rangos de filtro del catálogo ya usan "$4.990", "$9.990", etc.). Se cambió a `step: 1`. El modelo (`validates :precio, numericality: { greater_than: 0 }`) nunca tuvo el problema — es puramente una restricción del input HTML. **Nota para el futuro**: este tipo de bug (constraint HTML5 del lado del cliente) no lo detectan los tests de request/integración ni pruebas por `curl`, porque esa validación solo la aplica el navegador — hay que probarlo a mano en un browser real o con un test de sistema (Capybara+Selenium, que el proyecto ya tiene instalado pero sin usar) para agarrarlo.

- [x] **Código muerto eliminado: `AccesoController#bienvenida`** (2026-08-17). Esa acción (sin ninguna ruta que la apuntara) y su vista `app/views/acceso/bienvenida.html.erb` quedaron de una etapa temprana del desarrollo. De paso se borró también `app/views/acceso/entrar.html.erb`, que tampoco se usaba — la acción `entrar` nunca renderiza su propia vista (siempre redirige a la home o renderiza `:invalido`). Verificado con la suite completa (131 tests, sigue en verde) y a mano contra el server real que el flujo de acceso por token sigue funcionando igual.

- [x] **Fix: nombre de producto cortado con "..." en el catálogo** (2026-08-17). `.producto-nombre` tenía `white-space: nowrap; overflow: hidden; text-overflow: ellipsis` — con nombres reales como "Paté de Salmón Ahumado" o "Salmón ahumado pieza pequeña" (más largos que los de prueba originales), el texto se cortaba tipo "PATÉ DE SALMÓN AH...". Se cambió a `-webkit-line-clamp: 2` (dos líneas antes de cortar, en vez de una) y `.producto-card` pasó a `display: flex; flex-direction: column` con `.producto-precio` en `margin-top: auto` — así el precio queda siempre pegado al borde inferior de la card, sea que el nombre ocupe 1 o 2 líneas, y como CSS Grid ya empareja la altura de todas las cards de una misma fila, quedan alineadas entre sí sin superposición. Verificado que los 4 nombres reales del catálogo actual aparecen completos en el HTML renderizado y que la suite completa (131 tests) sigue en verde; **no se pudo confirmar visualmente el wrap a 2 líneas en un browser real** (sin Playwright/`chromium-cli` en este entorno, mismo motivo de siempre) — falta esa pasada visual. De paso: "Paté de Salmóm Ahumado" (producto id 4) tiene un typo — debería decir "Salmón", no "Salmóm" — no se tocó porque es contenido real del negocio, hay que corregirlo desde `/admin/products`.

- [x] **Fix: filas de las tablas del admin (Productos/Usuarios/Pedidos) desalineadas verticalmente** (2026-08-17, corregido de verdad — ver nota abajo). La celda de acciones (`.acciones-fila`, con los links "Editar"/"Eliminar") tenía `display: flex` puesto **directo en el `<td>`**. Primer intento (mismo día): agregar `vertical-align: middle` a la regla global `th, td` — **no alcanzaba**, porque `vertical-align` no tiene ningún efecto sobre un elemento con `display: flex` (deja de participar del modelo de layout de tabla como una celda normal, así que ese `<td>` quedaba con una altura/posición de borde distinta a la del resto de la fila, cortando la línea divisoria en un punto distinto). Joaquín lo confirmó visualmente: "no siguen la misma línea, hay una línea que se corta ahí". **Fix real**: se sacó `display: flex` del `<td>` y se movió a un `<div class="acciones-fila">` adentro de la celda — el `<td>` vuelve a ser una celda de tabla normal e indistinguible de sus vecinas, y el `<div>` interno sigue dando el layout en fila para "Editar"/"Eliminar". Aplicado en `admin/products/index.html.erb` y `admin/users/index.html.erb` (los 2 únicos lugares donde se usaba este patrón). Verificado con la suite completa (131 tests, sigue en verde) y el HTML renderizado a mano contra el server real.

- [x] **Auditoría de accesibilidad WCAG 2.1 AA + fixes** (2026-08-17). Joaquín preguntó si el sitio sigue las pautas W3C AA — no se había revisado nunca. Auditoría manual sobre el código (contraste calculado con la fórmula real de WCAG, no a ojo) más revisión de semántica/teclado. Se encontraron y corrigieron varios incumplimientos reales:
  - **Contraste de texto insuficiente (1.4.3, ratio mínimo 4.5:1)**: el gris `#888`/`#999` usado en ~25 lugares (fechas, hints de formulario, texto de tablas, badges "invitado", notas, etc.) medía solo 2.85–3.54:1. Se reemplazó por `#666` (5.3–5.7:1) de forma global en el CSS y en los estilos inline que quedaban sueltos en 3 vistas.
  - **Contraste de texto dorado sobre fondo claro**: `var(--gold)` (`#b8934f`) mide 2.87:1 sobre blanco/foam — falla feo. Funciona bien sobre fondos oscuros (navy, 5.88:1) porque así se diseñó originalmente, pero se usaba también como texto en 3 lugares con fondo claro: `.codigo-pedido` (pantalla de pedido recibido), `.valor-card h3` (tarjetas "Artesanal/Del sur/Directo a ti" en `/nosotros`) y el ícono de estrella llena de las reseñas. Se agregó una variable nueva `--gold-texto: #8a6d3b` (mismo dorado oscurecido, 4.85:1 sobre blanco — de hecho ya existía ese tono exacto en el código para `.datos-banco h2`, así que es consistente con algo que Joaquín ya había usado antes) y se aplicó en esos 3 lugares, más 2 estados `:hover` con el mismo problema (botón de copiar en la pantalla de transferencia, navegación del calendario de viernes). El ícono de estrella vacía (`#ddd`, 1.36:1) también se oscureció a `#666`.
  - **Estrellas de calificación sin nombre accesible (4.1.2)**: los radio buttons de "Tu calificación" en las reseñas solo tenían un ícono SVG (`aria-hidden`) dentro del label — un lector de pantalla no anunciaba nada útil. Se agregó texto oculto visualmente pero legible por lectores de pantalla (`<span class="sr-only">`, nueva clase utilitaria) con "1 estrella"… "5 estrellas".
  - **Campo de búsqueda del catálogo sin label (3.3.2/4.1.2)**: el input de "Buscar" solo tenía `placeholder` (no es un sustituto válido de label). Se agregó `aria-label="Buscar por nombre o descripción"`.
  - **Falta `alt` en una imagen (1.1.1)**: la preview de foto en el formulario de edición de producto del admin no tenía texto alternativo.
  - **Falta `lang="es"` en `<html>` (3.1.1, nivel A — ni siquiera llegaba a AA)**: ninguno de los 3 layouts (`application`, `admin`, `mailer`) tenía el idioma declarado, así que un lector de pantalla podía intentar leer el sitio en español con pronunciación de inglés.
  - **Calendario de viernes sin cerrar con Escape**: se agregó (`keydown.esc`), devolviendo el foco al botón que abrió el popup — antes solo se cerraba con click afuera.
  - Verificado con la suite completa (131 tests, sigue en verde) y contraste recalculado con la fórmula oficial de WCAG para cada combinación color/fondo tocada.
  - **Confirmado visualmente por Joaquín en un browser real (2026-08-17)**: navegación por teclado, lector de pantalla y el dorado oscurecido (`--gold-texto`) — todo OK, sin ajustes pedidos.

- [x] **Confirmado visualmente por Joaquín (2026-08-17)**: el wrap a 2 líneas de los nombres de producto en el catálogo, y el alineado de las filas en las tablas del admin (fixes de 2026-08-17) se ven bien en un browser real.

- [x] **Repaso de vistas página por página, arrancando por la Home** (2026-08-17). Empezamos a revisar el sitio página por página en orden de prioridad de negocio (Home → Carrito/Checkout → Cuenta → Admin → Devise poco usado). En la Home:
  - **Fix: botón "Explorar productos" con texto blanco sobre fondo dorado (2.87:1, falla WCAG AA)** — se me había pasado en la auditoría de accesibilidad anterior porque ahí solo revisé el dorado como color de *texto*, no como *fondo*. Se cambió el texto a `var(--navy)` (5.88:1) — mismo dorado vibrante, solo cambia el color de la letra.
  - **Cambio de tipografía en todo el sitio**: Georgia (serif) tenía numerales "old-style" (altura despareja, algunos números bajan de la línea) que se ven raros en un sitio con tantos precios — y a Joaquín tampoco le gustaba el tipo de letra en general. Se reemplazó por **Fraunces** (serif con carácter, para `h1`/`h2`/el logo — mantiene un toque premium/artesanal) + **Inter** (sans-serif limpia, para todo lo demás: texto, botones, tablas, precios — numerales siempre parejos). Cargadas desde Google Fonts en `application.html.erb` y `admin.html.erb` (con `preconnect` para no penalizar performance). `body` además suma `font-variant-numeric: lining-nums` por si algún elemento puntual quisiera Georgia/otra serif con old-style numerals en el futuro, para forzar números uniformes igual.
  - Verificado que el link de Google Fonts responde 200 y que la suite completa (131 tests) sigue en verde. **La revisión visual de la Home la está haciendo Joaquín directamente** (dijo que no necesita que se la muestre) — pendiente su feedback para seguir ajustando antes de pasar a la siguiente página.

- [x] **Rediseño grande de la Home: paleta de mar, nav flotante, hero simple, collage de productos, "Quiénes somos" nuevo** (2026-08-17). Joaquín pidió repensar la Home en 5 partes (nav, hero, productos, quiénes somos, footer) — footer se dejó igual a propósito ("no me interesa tanto"). Resumen:
  - **Paleta de colores**: se sacó el dorado (`--gold`/`--gold-texto`) de toda la hoja de estilos — no convencía y no representaba "mar". Nueva variable `--celeste: #5fbad9` (acento fresco sobre fondos oscuros: nav, hero, footer, badges) y se reforzó el uso de `--ocean` (#145577, ya existía) como el azul legible sobre fondos claros — mismo rol que cumplía `--gold-texto` antes. `--border` se afinó a un gris azulado (`#d5e3ea`) en vez del gris neutro anterior. Los 20 usos de dorado en el CSS se repasaron uno por uno según su fondo (oscuro → celeste, claro → ocean) para no romper contraste WCAG AA de nuevo.
  - **Nav (parte 1)**: ahora es `position: fixed` (antes `sticky`) y flota **sobre** el hero — en la Home arranca completamente transparente y pasa a `rgba(navy, 0.8)` apenas se hace scroll (`app/javascript/controllers/nav_scroll_controller.js`, agrega la clase `.scrolled` con `window.scrollY > 10`). En el resto de las páginas el nav es siempre sólido, como antes — se agregó una clase `nav-transparente` al `<body>` solo en `pages#inicio` para distinguir los dos comportamientos, y `padding-top` compensatorio en el resto de las páginas para que el nav fixed no tape contenido (el admin, que usa un nav distinto no-fixed, quedó explícitamente exceptuado). Letras del nav más grandes (logo 1.2rem→1.7rem, links 0.88rem→1rem) y se sacaron los separadores `|` (`border-left`/`border-right` de `.nav-center`).
  - **Hero (parte 2)**: se simplificó a imagen de fondo + "Fuyén Ahumados" + una frase en una línea + botón "Explorar productos" — se sacó el eyebrow "Artesanal · Patagonia". Imagen de fondo con degradé navy encima (mismo patrón que ya se usaba en el banner del catálogo) — **es un placeholder** (`public/docs/banners/home-hero.jpg`, copia de una foto de producto existente), falta la foto real.
  - **Collage de productos (parte 3)**: reemplaza la grilla de tarjetas por un collage de 2 cuadrados lado a lado — el primero con la foto grande de un producto, el segundo subdividido en 2×2 con los otros 3 productos más un botón "Compra aquí" en el cuadrante restante. Con los 4 productos reales actuales calza justo (1 grande + 3 chicas + 1 botón).
  - **Quiénes somos (parte 4)**: layout nuevo de foto a la izquierda / texto a la derecha (antes era una sección oscura centrada con texto largo). Título "Quiénes somos" en dos colores (navy + ocean). Copy nuevo, más relajado, con la idea que dio Joaquín (tres amigos, buena comida, pesca, juntarse) — reemplaza el texto anterior sobre "empresa familiar / tradiciones patagónicas". Foto también placeholder (`public/docs/banners/quienes-somos.jpg`).
  - Verificado con la suite completa (131 tests, sigue en verde), CSS recompilado sin errores ni referencias rotas a `var(--gold)`, y el HTML de la Home, el catálogo, `/nosotros` y el admin revisados por `curl` para confirmar que el nav fixed no tapa contenido en ninguna página. **No se pudo ver el resultado real en un browser** (mismo motivo de siempre) — Joaquín lo está revisando directamente.
  - **Ajuste tras el primer feedback de Joaquín sobre "Quiénes somos"**: la foto ocupaba media sección estirada a `align-items: stretch` (podía llegar a verse enorme según el alto del texto) — se cambió a un tamaño fijo y más chico (`max-width: 380px`, proporción 4:5, esquinas redondeadas), columnas del grid ya no 50/50 sino 0.85fr/1.15fr (más espacio para el texto), y el título/párrafo pasaron de estar centrados verticalmente a alinearse arriba (`align-items: start` + `justify-content: flex-start`) — así el título queda a la misma altura que el borde superior de la foto, como pidió. El párrafo subió de 1.05rem a 1.3rem.

- [x] **Segunda ronda de feedback sobre la Home + corrección importante: no somos "del sur de Chile"** (2026-08-17). Joaquín aclaró que la marca no es del sur de Chile ni tiene que ver con la Patagonia — son 3 amigos, artesanal y premium. Se sacó esa frase de **todos** los lugares donde aparecía: footer, hero de la home, banner del catálogo, y la tarjeta "Del sur" de `/nosotros` (ahora "Premium", sin afirmar un origen geográfico específico). Además:
  - **Nav**: logo pasó de "FUYÉN" a "FUYÉN AHUMADOS" (sigue siendo el link a la home), el link "Nosotros" pasó a decir "Quiénes somos" (coincide con el título real de esa página), los links del nav ahora usan la tipografía de título (Fraunces) y todo el texto del nav es más grande (logo 1.2rem→1.5rem, links 0.88rem→1.15rem).
  - **Hero**: bajada simplificada a solo "Salmón ahumado artesanal." (antes mencionaba el sur de Chile). Botón nuevo `.btn-hero`: fondo navy, texto blanco, `border-radius: 999px` (óvalo/píldora) — antes era dorado.
  - **Collage de productos**: las fotos dejaron de ser links al detalle del producto (ahora son `<div>`, no `<a>` — "que sean solo fotos"). El cuadrante "Compra aquí" se sacó — como solo hay 4 productos reales (1 grande + 3), no había una 5ta foto para llenarlo sin duplicar una imagen, así que el grid de la derecha pasó de 2×2 a 3 filas apiladas en una columna, mostrando los 3 productos restantes sin espacios vacíos ni fotos repetidas. Bajada nueva bajo el título (antes "Piezas elaboradas artesanalmente, una a una.", ahora una más entretenida sobre receta propia y cuidado del detalle). Botón "Ver todos los productos" sin flecha, nueva clase `.btn-navy-grande` (navy, texto blanco negrita, más grande y ancho que el botón secundario anterior).
  - **Quiénes somos**: el título dejó de tener la palabra "somos" en un segundo color (`--ocean`) — ahora todo el título es un solo color navy. El botón "Conocer nuestra historia" (antes con flecha y estilo secundario) pasó a `.btn-navy-grande`, igual que el de productos.
  - **Footer**: logo y los `<h4>` ("Navegación", "Contacto") ahora usan la tipografía de título (Fraunces), un poco más de aire entre columnas (`gap` 2rem→2.5rem) y la bajada bajo el logo cambió de "Salmón ahumado artesanal del sur de Chile" a "Salmón ahumado artesanal y premium." El link "Nosotros" del footer también pasó a decir "Quiénes somos".
  - Verificado con la suite completa (131 tests, sigue en verde), CSS recompilado sin errores, y el HTML de la Home/`/nosotros` revisado por `curl` para confirmar cada cambio puntual (texto del nav, botones sin flecha, collage sin links, etc.). **No se pudo ver el resultado real en un browser** — Joaquín lo sigue revisando directamente.

- [x] **Tercera ronda de feedback sobre la Home** (2026-08-17). Ajustes puntuales sobre lo anterior:
  - **Nav**: "Carrito"/"Iniciar sesión"/el nombre del cliente logueado ahora son más grandes (`.nav-right` 0.88rem→1.05rem, `.nav-nombre` 0.82rem→0.95rem) y todo el texto del nav (`.nav-center` y `.nav-right`) usa `text-transform: capitalize` para que cada palabra arranque con mayúscula ("Iniciar Sesión", "Nuestros Productos", etc. — vía CSS, no se tocó el texto real). `.nav-nombre` ahora trunca con "…" si el nombre del cliente es muy largo (`max-width: 160px` + `text-overflow: ellipsis`) para no romper el layout del nav. De paso until se corrigió un contraste roto: `.nav-nombre` estaba en `#666` sobre fondo navy (2.94:1, bajo el mínimo AA) — quedó en `#aaa` (7.26:1, mismo gris claro que ya se usa en el footer sobre navy).
  - **Hero**: se sacó el punto final de "Salmón ahumado artesanal" y esa frase subió de 1.2rem a 1.5rem. El botón volvió a ser rectangular (Joaquín se arrepintió del óvalo de la ronda anterior) y más ancho (`padding` horizontal 2.5rem→4rem).
  - **Nuestros productos**: se reescribió el párrafo de bajada sacando el guion largo que tenía en el medio ("— porque si no nos convence..."), en su lugar quedó como una oración seguida seguida. El título ("Nuestros productos") pasó a tener `font-family` explícito a Fraunces (por si algo lo estaba pisando) y subió de peso/tamaño (400→600, 1.6rem→1.8rem) para que se note más claramente que es tipografía de título.
  - **Quiénes somos**: se agregó `background: #eaedf0` (gris claro) a la sección — antes usaba el mismo `--foam` que la sección de productos y las dos se veían pegadas sin ninguna separación visual.
  - Verificado con la suite completa (131 tests, sigue en verde) y CSS recompilado sin errores. **No se pudo ver el resultado real en un browser** — Joaquín lo sigue revisando directamente.

- [x] **Link real de Instagram en el footer** (2026-08-17). Antes era texto plano "Instagram: @fuyen" (ni siquiera el handle real). Ahora es un link de verdad a `https://www.instagram.com/fuyenahumados_/` (URL canónica, sin el parámetro `igsh` de tracking de share que traía el link que pasó Joaquín — no hace falta para un link permanente del sitio), `target="_blank" rel="noopener"`, con el handle correcto `@fuyenahumados_`.

- [x] **Cuarta ronda: sistema de tipografía consistente en toda la Home** (2026-08-17). Joaquín notó que "Carrito"/"Iniciar sesión" se veían con una tipografía distinta al resto del nav, y en general pidió que hubiera "un patrón que se repita" en vez de tamaños sueltos por sección. Se armó una escala clara y se aplicó de punta a punta:
  - **Bug real: `.nav-right` nunca tenía `font-family: Fraunces`** — cuando le agregué la tipografía de título a `.nav-center` (ronda anterior), me olvidé de aplicarla también a `.nav-right` ("Carrito", "Iniciar sesión", el nombre del cliente), que seguía heredando Inter del body. Por eso se veían distintos. Corregido — ahora los dos usan Fraunces al mismo tamaño (1.15rem).
  - **Escala de tipografía de título (Fraunces)** aplicada de forma consistente: hero `clamp(2.75rem, 6vw, 4.5rem)` → títulos de sección (`.seccion-titulo` y `.quienes-somos-titulo`, ahora **iguales**: 1.8rem / peso 600 / mismo letter-spacing — antes "Quiénes somos" estaba en 2.4rem, se notaba descuadrado contra "Nuestros productos" en 1.8rem) → bajada del hero "Salmón ahumado artesanal" (1.5rem, antes en Inter, ahora en Fraunces) → nav (1.15rem).
  - **Párrafos de descripción (Inter) unificados**: la bajada de "Nuestros productos" (`.seccion-subtitulo`) y el párrafo de "Quiénes somos" (`.quienes-somos-texto .seccion-texto`) eran dos clases distintas con tamaño, color y estilo distintos (1rem itálica gris `#666` vs 1.3rem sin itálica gris `#555`) — quedaron iguales: 1.2rem, `#555`, `line-height: 1.8`, sin itálica.
  - **Botón del hero**: letra más grande (0.95rem→1.15rem) y más ancho (padding horizontal 4rem→5rem) — Joaquín seguía sin quedar conforme con versiones anteriores.
  - Verificado con la suite completa (131 tests, sigue en verde) y CSS recompilado confirmando los valores exactos de cada regla. **No se pudo ver el resultado real en un browser** — Joaquín lo sigue revisando directamente.

- [x] **Quinta ronda: texto de "Quiénes somos" más desarrollado, línea separadora entre secciones, logo del footer sin mayúscula sostenida** (2026-08-17).
  - **"Quiénes somos"**: el párrafo pasó de 3 líneas a dos párrafos más desarrollados, contando cómo el proyecto empezó como algo casero (ahumar para las juntas propias) hasta decidir compartirlo con más gente — mismo tono relajado, mismos 3 amigos, sin usar guiones largos (Joaquín ya había pedido sacarlos del párrafo de productos).
  - **Línea separadora entre secciones**: se agregó `border-top: 1px solid #000` a `.seccion` (separa hero↔productos y productos↔"Quiénes somos") y al `footer` ("Quiénes somos"↔footer) — una línea negra angosta en cada quiebre de sección de la Home.
  - **Logo del footer**: estaba "FUYÉN AHUMADOS" en mayúscula sostenida (texto literal en el HTML, no un `text-transform` de CSS) — se cambió a "Fuyén Ahumados". El logo del nav (arriba) se dejó igual a propósito, Joaquín solo pidió el cambio para la sección 5 (footer).
  - Verificado con la suite completa (131 tests, sigue en verde) y el HTML/CSS revisado por `curl` para confirmar el texto exacto y las reglas de borde. **No se pudo ver el resultado real en un browser** — Joaquín lo sigue revisando directamente.

- [x] **Sexta ronda: ícono de Instagram en el nav, mayúscula por palabra en los botones, ajustes de ancho/color** (2026-08-17).
  - **Ícono de Instagram en el nav**: nuevo, al final de `.nav-right` (después de "Iniciar sesión" o "Salir", según el estado de sesión) — SVG de línea propio (cuadrado redondeado + lente + flash), mismo estilo que los íconos de carrito/perfil que ya existían, mismo tamaño que el del carrito (19px) para que no se vea desproporcionado. Linkea a `https://www.instagram.com/fuyenahumados_/`, con texto oculto para lectores de pantalla ("Fuyén en Instagram").
  - **Mayúscula por palabra en los botones**: se agregó `text-transform: capitalize` a `.btn-hero` y `.btn-navy-grande` — cubre los 3 botones principales de la Home ("Explorar Productos", "Ver Todos Los Productos", "Conocer Nuestra Historia") de una sola vez, mismo patrón que ya se usaba en el nav.
  - **Botón del hero más grande**: letra de 1.15rem a 1.3rem.
  - **Botón "Ver todos los productos" del mismo ancho que el collage de fotos**: antes era un botón normal centrado; ahora `.seccion-cta .btn` es `display: block; width: 100%`, así ocupa exactamente el ancho de `.seccion-inner` — el mismo que ocupa `.productos-collage` arriba.
  - **Fondo de "Quiénes somos" más oscuro**: de `#eaedf0` a `#d8dcdf` (gris más marcado, contraste verificado: navy 12.22:1, texto gris `#555` 5.4:1 — sigue pasando AA cómodo).
  - Verificado con la suite completa (131 tests, sigue en verde), CSS recompilado, y el HTML del nav en sesión de invitado y de cliente logueado revisado por `curl` para confirmar el orden "Carrito → Iniciar sesión/Salir → Instagram". **No se pudo ver el resultado real en un browser** — Joaquín lo sigue revisando directamente.
- [x] **Séptima ronda: tamaños del footer unificados, texto "Mail" y "Conoce nuestra historia", botones al mismo tamaño** (2026-08-17).
  - **Footer con tipografía despareja**: `.footer-col h4` ("Navegación", "Contacto") estaba en 1rem mientras `.footer-logo` ("Fuyén Ahumados") estaba en 1.3rem, dando una jerarquía desordenada. Se unificó `.footer-col h4` a `1.3rem` para que coincida con el logo.
  - **"Reclamos" → "Mail"**: en `app/views/layouts/application.html.erb`, la etiqueta del correo de contacto en el footer decía "Reclamos:" y ahora dice "Mail:".
  - **"Conocer nuestra historia" → "Conoce nuestra historia"**: corregido a modo imperativo en el botón de la sección "Quiénes somos" (`app/views/pages/inicio.html.erb`).
  - **Botones del mismo tamaño**: `.btn-hero` había quedado en 1.3rem tras una ronda anterior, mientras `.btn-navy-grande` (botón de referencia, "Ver todos los productos") seguía en 1.05rem. Se bajó `.btn-hero` a `1.05rem` para que los 3 botones principales de la Home usen el mismo tamaño de letra.
  - Verificado con la suite completa (131 tests, 416 assertions, 0 failures, 0 errors, 98.94% cobertura), CSS recompilado, y el HTML revisado por `curl` en sesión de cliente para confirmar que aparece "Mail:" y "Conoce nuestra historia" en vez de los textos anteriores. **No se pudo ver el resultado real en un browser** — Joaquín lo sigue revisando directamente.

- [x] **Nueva página "Preguntas frecuentes"** (2026-08-18, a pedido de Joaquín). Tercera página de contenido del sitio, junto a "Quiénes somos" y "Nuestro proceso". Nueva ruta `GET /preguntas-frecuentes` (`pages#preguntas_frecuentes`, mismo patrón que `inicio`/`nosotros`), link agregado al final de "Navegación" en el footer (`app/views/layouts/application.html.erb`), después de "Consultar mi pedido" — no se agregó al nav superior, solo al footer, tal como se pidió. Página simple: 5 preguntas con respuesta corta (sin acordeón/JS) más una nota de cierre invitando a contactar por WhatsApp o mail si quedan dudas. Preguntas: cuánto dura el producto, en qué formato viene (kilo completo), cómo se paga (transferencia coordinada por WhatsApp), cuándo y dónde se despacha (viernes, 5 comunas), y cuánto cuesta el envío ($2.000, gratis desde $40.000) — estas últimas 4 con datos ya documentados en el sitio, sin inventar nada.
  **Nota sobre la pregunta de duración**: Joaquín no estaba seguro del dato exacto (dijo "refrigerado 2 meses, congelado 4 meses, pero no sé con certeza") — antes de publicar algo relacionado con seguridad alimentaria sin confirmar, se buscó una referencia (USDA FoodKeeper) para salmón ahumado sellado al vacío, que da rangos más conservadores: 14–45 días refrigerado sin abrir según el tipo de ahumado, 3–4 días una vez abierto, y hasta 3–6 meses congelado. Joaquín optó por publicar el rango conservador de esa referencia en vez de su estimado original. Texto final: "hasta 3 semanas refrigerado sin abrir, 3 a 4 días una vez abierto, hasta 3 meses congelado, consumir de inmediato tras descongelar y no volver a congelar". **Si en algún momento se confirma el dato real de conservación del proveedor/envasador, hay que actualizar `app/views/pages/preguntas_frecuentes.html.erb`** con el número real en vez de la referencia genérica.
  Verificado con la suite completa (132 tests — se agregó `test/integration/pages_test.rb#la página de preguntas frecuentes carga` —, sigue en verde, 98.95% cobertura) y el HTML renderizado contra el server real por `curl` (la página carga, el link del footer apunta bien).
  **Corrección importante (2026-08-18)**: la primera versión de la pregunta de formato decía "vendemos por kilo completo, no en fracciones" — Joaquín aclaró que eso es incorrecto, la venta es **por pieza/porción**, no por kilo. Se corrigió tanto la pregunta frecuente como el dato equivalente en la sección "Qué es esto" de este README (que tenía la misma afirmación errónea desde el origen del proyecto). Texto real, confirmado contra `Product` en la base: pieza pequeña (150–250 g aprox.), pieza grande (250–350 g aprox.), salmón ahumado desmenuzado (250 g) y paté de salmón ahumado (500 g) — los 4 productos ya cargados hoy, con su `peso_descripcion` real.
  **Fuente de los tiempos de conservación**: a pedido de Joaquín se agregó como link en la propia respuesta (no solo en este README). Se ajustó además el número publicado al de la fuente más confiable encontrada: la [Cold Food Storage Chart de FoodSafety.gov](https://www.foodsafety.gov/food-safety-charts/cold-food-storage-charts) (sitio oficial conjunto USDA/FDA/CDC/HHS) — "pescado ahumado, envasado al vacío": 14 días en refrigerador, 2 meses en congelador. Es más conservador que el rango usado en la primera versión de esta pregunta (que salía de blogs de terceros citando de forma más laxa a USDA), así que se reemplazó por completo. Referencia adicional consultada (no usada para el número final, pero confirma que el orden de magnitud es razonable para salmón ahumado sellado al vacío específicamente): [USDA ARS — Keeping In-Demand Smoked Salmon Safe to Eat](https://www.ars.usda.gov/news-events/news/research-news/2010/keeping-in-demand-smoked-salmon-safe-to-eat/) (3 a 8 semanas refrigerado).

- [x] **Recuperar contraseña + fix de mensaje de error roto en Devise** (2026-08-19, a pedido de Joaquín). Joaquín notó dos cosas: (1) no había manera de recuperar la contraseña si se olvidaba, y (2) un error de registro (contraseña o email mal escritos) mostraba un mensaje feo.
  - **(2) resultó ser un bug real, no solo un tema de estilo**: `devise/shared/_error_messages.html.erb` (el partial que muestra los errores en registro/login/recuperar contraseña) mostraba literalmente **"Translation missing: es.errors.messages.not_saved"** como encabezado — esa clave la trae `devise` en inglés (`config/locales/devise.en.yml`, generado en algún momento con `rails g devise:i18n:locale`) pero nunca existió una versión en español, y `rails-i18n` no la cubre porque es una clave propia de Devise, no de Rails/ActiveRecord. Además el nombre de "Password confirmation" tampoco estaba traducido, mostrando "Password confirmation no coincide" en vez de en español. Se corrigieron ambas cosas de raíz:
    - **`config/locales/devise.es.yml` nuevo**: traducción completa de los mensajes propios de Devise (mismas claves que ya existían en `devise.en.yml`) — asuntos de email, mensajes de fallo de login, etc. La mayoría no se ven en el sitio porque los flashes automáticos de Devise siguen desactivados, pero el asunto del email de recuperación sí se ve siempre.
    - **`es.activerecord.attributes.user`** nuevo en `config/locales/es.yml`: nombre, apellido, email, teléfono, contraseña, confirmación de contraseña — así los mensajes de error usan el nombre del campo en español.
    - **`_error_messages.html.erb` reescrito**: se sacó el encabezado que dependía de la clave rota y se cambió a la misma caja de error que ya se usa en el resto del sitio (`.form-errores`, ya tenía estilos) — antes era un `#error_explanation` sin ningún CSS, se veía como HTML sin estilar.
  - **(1) Recuperar contraseña**: la funcionalidad de Devise (`:recoverable`) ya estaba habilitada en el modelo `User` y las rutas/vistas base ya existían (generadas en algún momento pero nunca conectadas ni traducidas) — el problema era que no había ningún link visible hacia esa pantalla. Se agregó "¿Olvidaste tu contraseña?" en el login (`devise/sessions/new.html.erb`), y se tradujeron y restyled `devise/passwords/new.html.erb`/`edit.html.erb` al mismo patrón `.auth-card` que ya usan login/registro (antes eran el scaffold de Devise en inglés, sin estilo). También se tradujo el email que recibe el cliente (`devise/mailer/reset_password_instructions.html.erb`).
  - **Importante, no resuelto acá**: el envío real del email de recuperación (y cualquier otro email de Devise) todavía no va a llegar a ningún lado en producción — no hay SMTP configurado (`config.action_mailer.smtp_settings` sigue comentado en `config/environments/production.rb`). El flujo funciona de punta a punta a nivel de código (se genera el token, se intenta enviar), pero hasta que Joaquín no configure un proveedor de email (SendGrid, Postmark, Gmail SMTP, etc.) el cliente nunca va a recibir el link. Se agregó como pendiente explícito antes de sacar el sitio del modo privado (ver más abajo).
  - Verificado con la suite completa (146 tests, 458 assertions, sigue en verde — 4 tests nuevos: error de registro sin "Translation missing" y con el nombre de campo traducido, el link aparece en el login, pedir recuperación con un email real encola un correo (`assert_emails`) y redirige, y con un email inexistente no encola nada y muestra el error sin romper), Rubocop y Brakeman limpios, y probado de punta a punta contra el server real por `curl`: el link aparece en el login, el formulario de recuperación carga y envía, y se confirmó que el `reset_password_token` se generó de verdad en la base para el usuario.

- [x] **Login con Google, código completo — falta solo que Joaquín cree las credenciales** (2026-09-01, a pedido de Joaquín, ver decisión de diseño más abajo bajo "Pendiente"). Botón "Continuar con Google" (logo oficial de 4 colores, mismo patrón visual del botón clásico de Google, blanco con borde gris — a propósito **no** usa los colores del sitio, es la convención de marca de Google) arriba del formulario de login y de registro, separados por un divisor "o".
  - **Devise + `omniauth-google-oauth2`** (gemas nuevas: `omniauth-google-oauth2`, `omniauth-rails_csrf_protection` — esta segunda es obligatoria desde Rails 7/Devise recientes, protege el endpoint que inicia el flujo contra CSRF). `User` suma `:omniauthable, omniauth_providers: [:google_oauth2]` a la lista de módulos de Devise. Columnas nuevas `users.provider`/`users.uid` (migración `AddOmniauthToUsers`, índice único combinado).
  - **`User.from_google(auth)`**: busca por `provider`+`uid`; si no existe, busca por email — si ya había una cuenta creada por email/password con ese correo, la **vincula** (le setea `provider`/`uid`) en vez de crear una duplicada; si tampoco existe por email, crea un `User` nuevo con rol `cliente`, contraseña aleatoria (`Devise.friendly_token`, el usuario nunca la usa a menos que después pida "recuperar contraseña"), y nombre/apellido que entrega Google.
  - **`telefono` ya no es obligatorio para una cuenta con `provider` seteado** (`validates :telefono, presence: true, unless: -> { provider.present? }`) — Google no entrega ese dato, y a pedido explícito de Joaquín no debía agregarse fricción pidiéndolo en el login. El formato sigue validado si el campo se llega a completar (`allow_blank: true` en esa validación separada). Sigue sin haber ningún lugar del sitio que le recuerde a un cliente de Google que le falta el teléfono — si en algún pedido hace falta, hoy lo escribe a mano en el checkout (el campo de contacto del pedido es independiente del perfil).
  - **`Users::OmniauthCallbacksController#google_oauth2`** hace el login real (`sign_in_and_redirect`); `#failure` (si Google devuelve un error o el usuario cancela) vuelve al login.
  - **El botón solo se muestra si `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` están seteadas** (`Devise.omniauth_configs.key?(:google_oauth2)` en el partial `devise/shared/_google_button.html.erb`) — hoy esas variables no existen en ningún entorno, así que el botón está invisible en desarrollo y en producción hasta que Joaquín las configure. **Para activarlo**: crear un proyecto en [Google Cloud Console](https://console.cloud.google.com/) → APIs & Services → Credentials → OAuth client ID (tipo "Web application"), con el URI de redirección autorizado `https://<dominio-del-sitio>/users/auth/google_oauth2/callback`, y setear `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` como variables de entorno en Render (mismo mecanismo que `WHATSAPP_BUSINESS_NUMBER`).
  - Verificado con la suite completa (**172 tests, 552 assertions**, sigue en verde — 4 tests nuevos: registro nuevo vía Google sin teléfono, login repetido no duplica la cuenta, cuenta existente por email/password se vincula en vez de duplicarse, y la validación de teléfono relajada solo para cuentas con `provider`), Rubocop y Brakeman limpios, y probado de punta a punta simulando el flujo de OmniAuth en modo test (`bin/rails runner` con `OmniAuth::AuthHash`) más contra el server real por `curl`: el botón aparece/desaparece según si las credenciales están seteadas, y la ruta `/users/auth/google_oauth2` apunta bien. **No se pudo probar el flujo real contra la cuenta de Google de verdad** (no hay credenciales todavía) ni confirmar visualmente el botón en un browser real (mismo motivo de siempre) — falta que Joaquín cree las credenciales y lo pruebe entrando de verdad con una cuenta de Google.

## Pendiente

### 🔒 Último paso, sin excepción: sacar el sitio del modo privado

**Confirmado 2026-08-17: esto se hace estrictamente al final, después de que todo lo demás en esta
lista esté terminado — no antes.** Joaquín quiere entregar una página perfecta antes de sacarla del
modo privado y dejarla de acceso público. Hasta entonces, el sitio sigue exactamente como está:
solo accesible vía link con token secreto (`/acceso/:token`).

Cuando llegue el momento (y no antes), esto implica:
- Sacar el gate de `AccessToken` / `ApplicationController#verificar_acceso` que hoy exige el token en cada request.
- Setear `WHATSAPP_BUSINESS_NUMBER` en las variables de entorno de Render con el número real del negocio.
- Setear `BUSINESS_EMAIL` en Render con `ventasfuyen@gmail.com`.
- Revisar que no quede ninguna credencial de prueba (`db/seeds.rb`) pensada para un entorno privado.
- **Configurar un proveedor de SMTP** (`config.action_mailer.smtp_settings` en `config/environments/production.rb`, hoy comentado) — sin esto, "Recuperar contraseña" nunca le va a llegar el email al cliente. Cualquier proveedor sirve (SendGrid, Postmark, Gmail SMTP); las credenciales van en `bin/rails credentials:edit`, no en el código.

### Pendiente: Publicación del sitio en internet

- [ ] Quitar el middleware/filtro de acceso privado por token (before_action que
      redirige si no hay token válido) para que el sitio sea de acceso público
- [ ] Verificar que catálogo, checkout y home funcionen sin token en ventana de
      incógnito tras el deploy
- [ ] Comprar dominio propio (ej. .cl en NIC Chile, o .com)
- [ ] Configurar Custom Domain en Render (Settings → Custom Domains) y apuntar
      los registros DNS (CNAME/A) según indique Render
- [ ] Crear cuenta en Google Search Console y verificar la propiedad del dominio
- [ ] Generar sitemap.xml (evaluar gema `sitemap_generator`) y subirlo a Search Console
- [ ] Agregar robots.txt en public/ permitiendo el rastreo (Allow: /)
- [ ] Revisar que cada página tenga <title> y meta description únicos para SEO
- [ ] Solicitar indexación manual de la home en Search Console para acelerar el
      proceso (puede tardar días a semanas de forma orgánica)
- [ ] **Login con Google — falta que Joaquín cree las credenciales en Google Cloud** (código completo, ver entrada en "Estado actual" más arriba). Sin `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` seteadas, el botón queda invisible y nadie nota que falta.

### Todo lo que falta antes de eso

- [x] **Revisión de vistas view-por-view, en este orden** (2026-08-17, completada 2026-08-25) — misma auditoría de tipografía/colores/paleta que se hizo en Home (título Fraunces 1.8rem/600/navy, cuerpo Inter 1.2rem/#555, botones 1.05rem/bold/capitalize, ancho de contenedor consistente), aplicada al resto del sitio. Los 8 puntos de abajo ya están cerrados:
  1. [x] Quiénes somos — corregido: `.hero-eyebrow` (el texto "Nuestra historia" arriba del título) usaba una clase que no existía en el CSS, salía sin estilo. Se agregó siguiendo el mismo patrón "eyebrow" de las tarjetas de valores de esa página. **Revisado de nuevo (2026-08-18)**: se comparó contra el patrón de la auditoría (título Fraunces 1.8rem/600/navy, cuerpo Inter 1.2rem/#555, contenedor 960px) y la página ya cumplía — `h1` global, `.nosotros-intro`/`.valor-card p`/`.nosotros-historia p` y `.container` ya coincidían, no hizo falta ningún cambio de estilo adicional. Se reemplazó el texto placeholder de "Nuestra historia" (`app/views/pages/nosotros.html.erb`) por contenido real (luego reestructurado, ver ítem siguiente). **Rediseño de la página completa (2026-08-18)**, a pedido de Joaquín: se definieron las dos secciones principales del sitio para contar la marca — "Quiénes somos" (esta página) y "Nuestro proceso" — y dentro de esta página pasaron a convivir como dos bloques con foto grande + párrafo, alternando el lado de la foto: **"Sobre nosotros"** (foto a la izquierda, texto a la derecha — historia de los tres amigos, mismo tono que la Home) y **"Nuestro proceso"** (foto a la derecha, texto a la izquierda — selección del salmón, ahumado lento con maderas nativas, receta propia, venta directa sin intermediarios). Título y párrafo de cada bloque respetan exactamente el formato de la sección "Quiénes somos" de la Home (`.quienes-somos-titulo`/`.seccion-texto`: Fraunces 1.8rem/600/navy y Inter 1.2rem/#555/line-height 1.8). Se sacaron las 3 tarjetas de "valores" (Artesanal/Premium/Directo a ti) y el bloque viejo de "Nuestra historia" — su contenido quedó absorbido por los dos bloques nuevos, evitando repetir lo mismo dos veces en la misma página. Fotos placeholder nuevas: `public/docs/banners/nuestro-proceso.jpg` (copia de una foto de producto existente, mismo mecanismo que los demás placeholders del sitio — reemplazar cuando haya foto real del proceso de ahumado). **Ajuste inmediato**: Joaquín pidió sacar también el bloque de arriba (eyebrow "Nuestra historia" + título "Quiénes somos" + párrafo intro) — quedan solo las dos secciones. El título de "Sobre nosotros" pasó a ser el `<h1>` de la página (antes lo era el título del bloque eliminado) para que la página siga teniendo un único `<h1>`, con el mismo estilo visual de siempre (la clase `.nosotros-seccion-titulo` ya define ese formato). CSS muerto de `.nosotros-hero`/`.hero-eyebrow`/`.nosotros-intro` eliminado.
  **Segundo ajuste (2026-08-18)**: título alineado con el borde superior de la foto (`align-items: start` en vez de `center` en `.nosotros-seccion-grid`), párrafos de ambas secciones ampliados, se sacaron los guiones largos como conectores y se revisó ortografía/tildes. Se instaló Playwright + Chromium en el entorno para intentar verificarlo con una captura real, pero el binario de Chromium no puede correr: faltan librerías del sistema (`libnss3`, `libnspr4`, `libasound.so.2`) y `sudo` pide contraseña que no está disponible en este entorno — sigue sin haber forma de confirmar visualmente acá, Joaquín lo revisa directo en su navegador.
  **Tercer ajuste (2026-08-18)**: columnas de foto/texto pasaron de 1fr/1fr a 0.9fr/1.1fr (foto más angosta, texto más ancho) en las dos secciones — mismo ancho de columna de texto en "Sobre nosotros" y "Nuestro proceso" (la variante invertida usa `grid-template-columns: 1.1fr 0.9fr` + `order` para que la foto quede angosta a la derecha en vez de a la izquierda, sin cambiar el ancho en píxeles de ninguna columna). El objetivo era que el bloque de texto no quede más alto que la foto — sin poder renderizar un browser real en este entorno (ver punto anterior) no se pudo medir en píxeles exactos, así que es la mejor estimación a ojo; si al verlo el texto sigue notoriamente más largo que la foto, avisar para ajustar la proporción o acortar un poco el texto.
  **Cuarto ajuste (2026-08-18)**: los párrafos de ambas secciones se acortaron (de 2 párrafos largos a 2 párrafos breves cada uno), manteniendo la misma idea y tono, sin guiones largos y con ortografía revisada.
  **Quinto ajuste — banner arriba + banda gris en "Nuestro proceso" (2026-08-19, a pedido de Joaquín)**: se agregó de vuelta un banner arriba de la página (`.nosotros-hero`, mismo patrón que el banner del catálogo `.catalogo-hero`: foto de fondo con degradé navy, breadcrumb "Inicio › Quiénes somos" y título) — placeholder `public/docs/banners/quienes-somos.jpg` (misma foto que ya se usaba en la sección "Sobre nosotros", reutilizada). El `<h1>` de la página pasó a vivir en este banner ("Quiénes somos"); "Sobre nosotros" bajó a `<h2>` para que la página siga teniendo un único `<h1>`. La sección "Nuestro proceso" ahora tiene el mismo fondo gris que la sección "Quiénes somos" de la Home (`#eaedf0`) — para lograrlo tuvo que salir de `.container` (960px centrado) y pasar a ser una banda a todo el ancho de la pantalla, como ya hace esa misma sección en la Home; "Sobre nosotros" se queda dentro de `.container`, sin banda. Verificado con la suite completa (146 tests, sigue en verde) y el HTML renderizado contra el server real por `curl`, confirmando el único `<h1>` y el orden banner → Sobre nosotros → banda gris con Nuestro proceso.
  2. [x] Carrito (2026-08-18) — revisado contra el mismo patrón (título Fraunces 1.8rem/600/navy, botones 1.05rem/bold/capitalize, cuerpo Inter, contenedor 960px estándar). Ya cumplía en todo: el `<h1>` y los botones (`.btn`) heredan los estilos base globales sin overrides que rompan el patrón, sin restos de dorado. **Ajustes tras feedback de Joaquín**: (1) en la columna izquierda, el nombre/peso/precio de cada producto quedaban centrados verticalmente contra la foto en vez de alineados arriba — se agregó `align-self: flex-start` a `.carrito-item-info` para que el nombre arranque a la misma altura que el borde superior de la foto (mismo criterio que se usó en Quiénes somos). (2) Se sacó la fila "Despacho: Viernes" de la tarjeta "Resumen de compra" (`carrito/_resumen.html.erb`) — dato redundante, ya no se elige un día fijo de despacho ahí. Se sacó también del modal "Producto añadido al carrito" (`carrito/_modal.html.erb`), que tenía la misma fila duplicada. (3) Se sacaron las flechas de los botones "Completar datos de envío →" y "← Seguir comprando" (ahora sin flecha). Verificado con la suite completa (132 tests, sigue en verde) y el HTML renderizado contra el server real por `curl`.
  3. [x] Datos de envío (checkout) (2026-08-18) — revisado contra el mismo patrón. Título/botones ya cumplían (`<h1>` global, `.btn` base). Se encontraron y corrigieron 3 cosas menores: (1) se sacaron las flechas de "← Volver al carrito" y "Confirmar pedido →" (mismo criterio recién aplicado en el Carrito). (2) Se reemplazaron 3 `style="color:#666"` inline (peso del producto en la tabla resumen, nota de envío gratis, detalle de dirección guardada) por clases CSS nuevas (`.checkout-item-peso`, `.checkout-envio-nota`, `.checkout-direccion-detalle`) — el resto del sitio ya no usa estilos inline, esta página se había quedado atrás en eso.
     **Reestructuración a pedido de Joaquín**: la página tenía el resumen de compra (tabla de productos) arriba y el formulario de datos abajo — se invirtió el orden y se organizó en dos secciones con su propio título: **"Completa la información para tu pedido"** (ahora el `<h1>` de la página — nombre, apellido, teléfono, email, dirección, fecha de entrega, instrucciones) y, debajo, **"Resumen de tu pedido"** (`<h2>`, tabla de productos + envío + total, más ahora también dirección de entrega, fecha de despacho e instrucciones — estos 3 últimos datos no existían antes en el resumen). Como la dirección/fecha/instrucciones se completan en el formulario de arriba y el pedido recién se crea al enviarlo, el resumen de abajo se actualiza **en vivo** con lo que el cliente va escribiendo/eligiendo arriba, sin recargar la página — controller Stimulus nuevo `app/javascript/controllers/checkout_resumen_controller.js`, escucha cambios en calle/comuna/dirección guardada/fecha (calendario de viernes)/notas y refleja el resultado en el resumen; la fila de "Instrucciones" solo se muestra si el cliente escribió algo. Verificado con la suite completa (132 tests, sigue en verde), sintaxis del controller JS validada, y el HTML renderizado contra el server real por `curl` confirmando que todos los `data-controller`/`data-action`/`data-target` quedan bien encadenados. **No se pudo probar la actualización en vivo en un browser real** (sin Chromium disponible en este entorno, mismo motivo de siempre) — falta que Joaquín confirme que al cambiar dirección/fecha/notas arriba, el resumen de abajo se actualiza solo.
     **Segundo ajuste (2026-08-18, botones)**: "Confirmar pedido" ahora es el botón grande y prominente (1.15rem, más padding, `.checkout-boton-confirmar`), arriba; "Volver al carrito" pasó a ser un link chico de texto debajo (reusa la clase `.carrito-resumen-seguir`, mismo estilo que "Seguir comprando" tenía antes en el carrito) — mismo patrón de jerarquía visual (1 acción principal grande + 1 link secundario chico) que ya se usa en el Carrito.
     **Checkeo de dirección en el backend + mapa mejorado (2026-08-18, a pedido de Joaquín)**: antes cualquier texto en "Calle y número"/"Comuna" se aceptaba tal cual, sin verificar que la dirección existiera de verdad.
     - **Servicio nuevo `app/services/geocodificador_service.rb`** (`GeocodificadorService.buscar(calle, comuna)`): consulta la API pública de geocodificación de Nominatim/OpenStreetMap (la misma que ya usaba el mapa de direcciones del registro/perfil, ahora también server-side) y devuelve si la dirección existe y, si existe, sus coordenadas. Si el servicio no responde (caída, timeout, límite de uso) devuelve `nil` **a propósito** — un pedido real no debe rechazarse por una falla de un servicio externo, solo cuando Nominatim respondió y confirmó que no encontró nada. Probado en este entorno contra la API real: funciona, pero requiere un header `User-Agent` identificable (`FuyenAhumados/1.0 (ventasfuyen@gmail.com)`, ya incluido) — sin eso Nominatim devuelve 403. **Ojo a futuro**: Nominatim es gratis pero tiene límite de uso (máx. 1 request/segundo) — a la escala actual del negocio (pocos pedidos por semana) no es un problema, pero si el volumen crece mucho habría que migrar a un servicio de geocodificación pago.
     - **`CheckoutController#create`** ahora llama a este servicio (solo si el pedido ya pasó las demás validaciones, para no gastar una consulta en un formulario a medio llenar) y rechaza el pedido con el error "no la pudimos encontrar, revisa que esté bien escrita" en el campo de calle si Nominatim confirma que la dirección no existe. Si la dirección sí existe, sus coordenadas quedan guardadas en el pedido.
     - **Columnas nuevas `lat`/`lng` en `orders`** (migración `AddLatLngToOrders`) — a diferencia de `Direccion` (donde `lat`/`lng` los puede setear el cliente arrastrando el pin en el mapa), en `Order` **nunca se confía en lo que mande el cliente**: se sobrescriben siempre con el resultado de la verificación del propio servidor. El mapa del formulario es solo una ayuda visual, no la fuente de verdad.
     - **Mapa "del registro, mejorado"**: se reutilizó el mismo controller Stimulus del mapa de direcciones (`direccion_map_controller.js`, Leaflet + Nominatim, sin API key) en el checkout — al escribir la calle o cambiar la comuna, un mapa (`.direccion-mapa`) muestra un pin que se puede arrastrar si no queda exacto, igual que en el registro/perfil. Se aprovechó para corregir un bug encontrado de paso: el pin todavía usaba el dorado viejo (`#b8934f`), color que se sacó de toda la paleta del sitio en el rediseño de la Home — se cambió a `--ocean` (`#145577`).
     - **Tests**: se agregaron 2 tests de integración nuevos (`test/integration/compra_test.rb`) — uno confirma que una dirección que Nominatim no encuentra rechaza el pedido con el mensaje esperado, otro confirma que si el servicio no responde el pedido igual se puede confirmar (comportamiento "fail-open" a propósito). Los 3 tests de checkout que ya existían se actualizaron para no depender de la red real (`stub_geocodificador`, helper nuevo en `test_helper.rb` que reemplaza `GeocodificadorService.buscar` durante el bloque sin tocar la red — **nota técnica**: Minitest 6 (la versión que usa este proyecto) sacó `Object#stub`/`minitest/mock` del core, así que en vez de sumar una gema nueva solo para esto se armó el helper a mano con `define_singleton_method`). Suite completa: **136 tests, 430 assertions, sigue en verde, 97.01% de cobertura**. Brakeman y Rubocop limpios sobre todos los archivos nuevos/tocados.
     **No se pudo probar visualmente el mapa ni el flujo del pin en un browser real** (mismo motivo de siempre, sin Chromium disponible) — falta que Joaquín confirme que el mapa aparece y el pin se mueve bien al escribir la dirección o arrastrar.
     **Ajuste tras feedback de Joaquín (2026-08-18)**: dos correcciones sobre este mismo punto.
     - **Título "Resumen de tu pedido" no coincidía con "Completa la información..."**: el `<h1>` de la página usa el patrón de título del sitio (Fraunces 1.8rem/600/navy), pero "Resumen de tu pedido" era un `<h2>` normal (Fraunces 1.3rem, sin color), heredado del estilo genérico de `h2` que usan otras páginas (carrito, perfil, devise) — no correspondía tocar ese estilo global porque rompería esas otras páginas. Se agregó una clase nueva y acotada (`.checkout-seccion-titulo`, mismos valores exactos que `h1`) aplicada solo a este `<h2>` puntual.
     - **Geocodificación poco confiable ("no pueden haber errores")**: `GeocodificadorService` armaba la consulta a Nominatim como un solo string libre (`"calle, comuna, Chile"` en el parámetro `q`), que Nominatim a veces interpreta mal — la comuna terminaba pesando como una palabra más del texto en vez de filtrar de verdad por ciudad, y una dirección real con un número de casa que no está cargado en OpenStreetMap (común en calles nuevas o pasajes chicos) se rechazaba como "no encontrada" aunque la calle sí existiera. Dos cambios, aplicados tanto en el servicio del backend como en el mapa del frontend (`direccion_map_controller.js`), para que ambos se comporten igual:
       1. **Query estructurada** en vez de texto libre: los parámetros `street`/`city`/`country` de la propia API de Nominatim (en vez de meter todo en `q`) hacen que la comuna filtre de verdad como ciudad, no como una palabra suelta.
       2. **Reintento sin el número de la calle** si la primera búsqueda no encuentra nada — si eso sí encuentra la calle, se acepta (las coordenadas quedan sobre la calle, el pin se puede ajustar a mano). Solo se rechaza definitivamente si ni con ni sin número aparece nada.
     Probado contra la API real de Nominatim (`bin/rails runner`, no un mock) con 3 casos: dirección real con número (encontrada al primer intento), dirección real con un número inventado que no existe en OSM (encontrada por el fallback sin número) y una calle inexistente de verdad (correctamente rechazada). Suite completa sigue en verde (136 tests, 430 assertions) y Rubocop limpio sobre los archivos Ruby tocados. **Sigue sin poder confirmarse visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín pruebe el mapa a mano con una dirección real de las 5 comunas.
     **Campo nuevo "Número de casa o departamento" (2026-08-19, a pedido de Joaquín)**: pensado tanto para departamentos ("Depto 56") como para casas dentro de un condominio ("Casa 12") — un dato que no siempre queda claro solo con "Calle y número". Columna nueva `orders.direccion_numero_depto` (migración `AddDireccionNumeroDeptoToOrders`), **obligatoria** (`Order` valida presencia, igual que `direccion_calle`). Aparece en el resumen en vivo de la derecha (`checkout_resumen_controller.js`) y en todos los lugares donde ya se mostraba la dirección de un pedido: mensaje de WhatsApp, Excel de despacho, detalle del pedido en el admin y pantalla de "mi pedido".
     **Ajuste (2026-08-19)**: la primera versión de este campo se decidió que no se guardara en `Direccion` (direcciones del perfil) — el cliente lo tendría que volver a escribir en cada compra. Tras aclarar con Joaquín que eso significaba que alguien con la misma dirección guardada de siempre igual iba a tener que retipear su depto/casa en cada pedido, se cambió el enfoque: **ahora se guarda y se prellena**, igual que calle/comuna, pero sigue editable en el checkout por si un pedido puntual va a otro lugar. Columna nueva `direcciones.numero_depto` (migración `AddNumeroDeptoToDirecciones`), también obligatoria (`Direccion` valida presencia). Se agregó al formulario compartido `direcciones/_fields.html.erb` (usado por "Nueva dirección"/"Editar dirección" del perfil **y también por el formulario de registro**, que ya pedía una dirección inicial vía `accepts_nested_attributes_for :direcciones` — se sumó `numero_depto` a los parámetros permitidos de Devise en `ApplicationController#configure_permitted_parameters`). Al elegir una dirección guardada en el checkout, `direccion_selector_controller.js` ahora también prellena este campo. Se muestra junto a calle/comuna en perfil, detalle de usuario en el admin, y el fallback de pedidos antiguos en el detalle admin de un pedido.
     **Corrección de texto (2026-08-19, a pedido de Joaquín)**: el label decía "Número de casa o depto" (abreviado) — como el campo sirve tanto para departamentos como para casas, y la abreviación podía leerse como "solo para depto", se cambió a "Número de casa o departamento" completo, en los dos formularios (checkout y perfil/registro).
     Verificado con la suite completa (138 tests, 443 assertions, sigue en verde — se extendieron los tests de campos obligatorios de `Order`/`Direccion` y se actualizaron los factories/tests existentes que armaban registros a mano, incluyendo el test de registro con dirección incluida), Rubocop y Brakeman limpios, y probado de punta a punta contra el server real por `curl`: un cliente nuevo se registra con "Depto 56" en su dirección, el perfil lo muestra, y al ir al checkout y elegir esa dirección guardada el campo aparece prellenado con "Depto 56" (editable).
     **Ajuste importante — pasó de obligatorio a opcional (2026-08-19, a pedido de Joaquín)**: Joaquín hizo notar el caso de una casa normal sin depto/condominio (dirección tipo "Cerro de Ramón 12334", sin ningún número aparte) — obligar a escribir algo ahí no tenía sentido, era forzar a duplicar la calle. `Order`/`Direccion` ya no validan presencia de `direccion_numero_depto`/`numero_depto` — el campo queda vacío si no aplica. Los dos labels pasaron a decir "(opcional)" y el hint se reescribió: "Solo si tu casa o depto tiene un número aparte del de la calle (...). Si tu dirección ya es completa tal cual, déjalo en blanco." Como esto podía dejar una coma colgando en todos los lugares que arman el string de dirección ("Calle 123, , Providencia"), se centralizó ese armado en un método nuevo `direccion_completa` (en `Order` y en `Direccion`, mismo nombre en los dos) que arma la dirección salteándose cualquier parte vacía — reemplaza la interpolación manual que había en `WhatsappOrderMessageService`, `PedidosExcelExportService`, `PedidoReciboService`, y las vistas de perfil/admin/checkout/"mi pedido". Verificado con la suite completa (142 tests, 445 assertions, sigue en verde — 2 tests nuevos por modelo: que sigue siendo válido sin el campo, y que `direccion_completa` arma bien el string con y sin el dato), Rubocop y Brakeman limpios.
     **Se sacó el mapa interactivo y se dejó de rechazar pedidos por geocodificación fallida (2026-08-19, a pedido de Joaquín)**: el mensaje de error "no la pudimos encontrar, revisa que esté bien escrita" resultaba muy estricto para un negocio donde el pago y la dirección igual se terminan de confirmar a mano por WhatsApp — no tenía sentido que el sitio bloqueara un pedido real por una limitación de Nominatim. Cambios:
     - `CheckoutController#create` ya no rechaza el pedido si la dirección no se encuentra — `ubicar_direccion` (antes `direccion_inexistente?`) es ahora **best-effort**: si Nominatim confirma y encuentra la dirección, guarda lat/lng de referencia; si no la encuentra o el servicio no responde, el pedido se confirma igual, sin lat/lng. Cualquier problema real de dirección se resuelve a mano por el negocio en la conversación de WhatsApp, no exigiéndoselo al cliente en el formulario.
     - **Se sacó el mapa interactivo** (Leaflet + pin arrastrable) del checkout y del formulario de direcciones (perfil/registro) — solo era ayuda visual, nunca fue la fuente de verdad de las coordenadas guardadas, y era la pieza más frágil de las tres (nunca se pudo confirmar visualmente en este entorno). Se eliminó `direccion_map_controller.js`, el CSS asociado (`.direccion-mapa`, `.mapa-pin`), el vendor `leaflet.js`, `leaflet.css` y el pin de Leaflet en `config/importmap.rb`/el layout — Leaflet ya no es una dependencia del proyecto.
     - Como consecuencia, `direcciones.lat`/`direcciones.lng` quedaron completamente sin uso (ya no hay ningún lugar que los escriba) — se eliminaron esas dos columnas (migración `RemoveLatLngFromDirecciones`). `orders.lat`/`orders.lng` sí se conservan — siguen siendo la fuente de verdad de la coordenada, ahora poblada en segundo plano sin bloquear nada.
     - Tests actualizados: el test "rechaza dirección que no existe" pasó a confirmar lo contrario (el pedido se crea igual, sin lat/lng), y se agregó un test nuevo confirmando que una dirección que sí se encuentra guarda lat/lng correctamente. Suite completa: **143 tests, 445 assertions**, Rubocop y Brakeman limpios.
     **Fix de bug de paso**: `ubicar_direccion` originalmente usaba `order.update_columns(...)`, que solo funciona sobre un registro ya guardado en la base — en `create` el pedido todavía no estaba guardado en ese punto del flujo (`Order.new`, no `Order.create`), así que hubiera reventado con `ActiveRecordError` en cualquier pedido donde Nominatim encontrara la dirección. Corregido a una simple asignación de atributos (`order.lat =`/`order.lng =`), que sí queda guardada después con `@order.save!` dentro de la transacción.
     **Tercera ronda de ajustes al formulario (2026-08-19, a pedido de Joaquín)**:
     - **La dirección guardada marcada por defecto ahora rellena los campos desde que carga la página**, no solo al hacer click en la opción. Antes, si el cliente tenía una dirección principal, los campos de calle/comuna/depto ya venían prellenados por el servidor, pero nada garantizaba que quedaran sincronizados con la opción realmente marcada del selector (por ejemplo, si el navegador restaura el estado de los radios desde el historial). `direccion_selector_controller.js` ahora aplica los datos de la opción marcada también en `connect()` (al cargar la página), no solo en el evento de click — así elegir otra dirección después nunca deja una combinación calle/comuna mezclada de dos direcciones distintas.
     - **La fecha de entrega dejó de venir preseleccionada al viernes más próximo** — el cliente tiene que elegirla a mano siempre, para no arriesgarse a confirmar sin querer la fecha más próxima. `CheckoutController#nuevo_pedido_prellenado` ya no le pasa un valor inicial a `fecha_entrega`; el calendario (`shared/_selector_viernes.html.erb`) ya sabía mostrarse vacío ("Selecciona un viernes") cuando no hay fecha, no hizo falta tocarlo.
     - **Recordatorio dinámico de campos obligatorios**: nuevo controller Stimulus `checkout_requisitos_controller.js`, escucha los 7 campos requeridos (nombre, apellido, teléfono, email, calle, comuna, fecha de entrega — el depto/casa quedó opcional, no cuenta) y muestra un aviso ("Antes de confirmar, completa: ...") arriba del botón de confirmar, listando en vivo solo los que todavía están vacíos; se oculta solo cuando ya no falta ninguno. No reemplaza la validación del servidor (que sigue siendo la que manda), es una ayuda visual para no llegar al final del formulario y recién ahí enterarse de qué falta.
     Verificado con la suite completa (145 tests, 446 assertions, sigue en verde), Rubocop y sintaxis JS validados, y confirmado por `curl` que la página ya no trae ninguna fecha preseleccionada y que el markup del recordatorio queda bien encadenado. **No se pudo confirmar visualmente en un browser real** el comportamiento en vivo del recordatorio ni la sincronización de la dirección marcada (mismo motivo de siempre) — falta que Joaquín lo pruebe a mano.
  4. [x] Pedido confirmado (2026-08-19, a pedido de Joaquín) — cambios en la pantalla que se muestra justo después de crear el pedido (`pedidos_publicos#show`, misma vista que usa también "Consultar pedido" — ver ítem 7, comparten HTML). Tres cambios, todos en el bloque "Último paso: confirma por WhatsApp" (solo visible si el pedido sigue `pendiente_pago`) salvo el de descarga:
     - **Se sacó el botón "← Volver al catálogo"** del fondo de la página — ya no hay ninguna acción de "volver" en esta pantalla.
     - **Reinicio automático a los 60 segundos**: nuevo controller Stimulus `app/javascript/controllers/pedido_reinicio_controller.js`, cuenta regresiva visible ("Esta página se reiniciará en 60s...") debajo del botón de WhatsApp, redirige a la home al llegar a 0. Solo corre en esta pantalla cuando el pedido todavía está `pendiente_pago` (es decir, cuando existe el botón de WhatsApp para apretar) — si alguien consulta un pedido ya pagado más adelante por `/mi-pedido`, esa cuenta regresiva no aplica, tiene sentido dejarlo quieto para que pueda leer el estado con calma.
     - **Botón nuevo "Descargar mi pedido"**: reemplaza al botón de "volver" en el mismo lugar. Genera y descarga el detalle completo del pedido (productos, subtotal, envío, total, dirección con el numero de depto/casa nuevo, fecha de entrega, datos de contacto) — ver el ajuste siguiente, pasó de `.txt` a PDF. Lógica en `app/services/pedido_recibo_service.rb` (mismo patrón que `WhatsappOrderMessageService`/`PedidosExcelExportService`, cada uno arma su propio texto/formato sin compartir código entre sí — es la convención que ya tenía el proyecto). Ruta nueva `GET /mi-pedido/:codigo/descargar`, sin necesidad de sesión (mismo criterio que el resto de "consultar pedido por código").
     Verificado con la suite completa (138 tests, 441 assertions, sigue en verde — se agregaron 2 tests de integración nuevos para la descarga: contenido correcto y código inexistente), Rubocop y Brakeman limpios, sintaxis del controller JS validada, y probado de punta a punta contra el server real por `curl` (pedido creado a mano por `rails runner`, la página muestra el botón de descarga y la cuenta regresiva, y `/descargar` devuelve un archivo válido con los datos correctos). **No se pudo confirmar visualmente en un browser real** que la cuenta regresiva se vea bien y redirija sola a los 60s (mismo motivo de siempre, sin Chromium disponible) — falta que Joaquín lo compruebe esperando el minuto completo una vez.
     **Segunda ronda (2026-08-19, a pedido de Joaquín)**: reordenamiento y cambio de formato de la descarga.
     - **Descarga pasó de `.txt` a PDF real**, con membrete y mensaje de agradecimiento de la marca. Se agregaron las gemas `prawn`/`prawn-table` (sin binario externo, a diferencia de `wicked_pdf` — importa para Render). `PedidoReciboService#pdf` arma: encabezado "Fuyén Ahumados", datos del pedido, tabla de productos con precio unitario/subtotal, subtotal/envío/total, dirección de entrega, instrucciones si las hay, datos de contacto, y un cierre: *"¡Gracias por preferir Fuyén! Tu compra significa mucho para nosotros: cada pedido lo ahumamos con el mismo cuidado con el que lo haríamos para nuestra propia mesa. ¡Nos vemos el día de la entrega!"*. Verificado generando un PDF real contra un pedido de prueba e inspeccionando el texto extraído (con la gema `pdf-reader`, instalada solo para esta verificación puntual, no es dependencia del proyecto) — confirma que los acentos y la ñ salen bien (las fuentes base de Prawn cubren español vía Windows-1252, no hace falta una fuente TTF externa).
     - **El botón de WhatsApp ("Último paso: confirma por WhatsApp") pasó a ser lo primero de la página**, antes del código de pedido/estado — es la acción principal, no tenía sentido que quedara después.
     - **Nueva sección "Resumen de tu pedido"** (reemplaza el bloque de texto suelto que tenía "Día de entrega"/"Dirección de entrega"/"Instrucciones"): número de pedido, tabla de productos (con precio y envío, reutilizando el partial `pedidos/_items` que ya existía), fecha de entrega, dirección de entrega e instrucciones si las hay — mismo estilo que la sección homónima del checkout (`.checkout-seccion-titulo`). El botón de descarga quedó al final de esta sección.
     Verificado con la suite completa (145 tests, 446 assertions, sigue en verde), Rubocop y Brakeman limpios, y probado de punta a punta contra el server real: PDF descargado y verificado con `pdf-reader`, orden de la página confirmado por `curl` (whatsapp-cta → código/estado → resumen).
     **Ajuste (2026-08-19)**: se había agregado un link de descarga del PDF dentro del mensaje de WhatsApp (`wa.me` no admite adjuntar archivos, solo texto, así que era la única forma de que el negocio recibiera el PDF por ese canal) — Joaquín pidió sacarlo después de verlo (mostraba la URL completa de `localhost` en el mensaje, se veía mal). `WhatsappOrderMessageService` volvió a su forma original, sin el parámetro `descarga_url`. El botón "Descargar mi pedido (PDF)" en la pantalla de pedido confirmado se mantiene igual, solo se sacó la mención dentro del mensaje de WhatsApp.
     **Tercera ronda (2026-08-19, a pedido de Joaquín, tras ver la página con datos reales)**:
     - **"Resumen de tu pedido" pasó a ser colapsable**: el título queda siempre visible, con un botón (chevron "v" que gira 180° al abrir, mismo ícono clásico de "desplegar") — el resto del contenido (número de pedido, fecha, dirección, instrucciones, tabla de productos, botón de descarga) arranca oculto y se muestra/oculta al hacer click. Controller Stimulus nuevo y genérico `colapsable_controller.js` (targets `contenido`/`boton`, una sola acción `toggle`) — no es específico de esta pantalla, se puede reutilizar en cualquier otra sección colapsable a futuro.
     - **CTA de WhatsApp más notorio**: fondo pasó de blanco a un verde muy pálido (`#f0fdf4`), borde más grueso (2px→3px), sombra suave (`box-shadow` verde), título y botón más grandes (título 1.7rem, botón 1.25rem con más padding). Sigue siendo lo primero de la página.
     - **La cuenta regresiva de 60s ahora arranca recién cuando el cliente aprieta el botón de WhatsApp**, no apenas carga la página — antes empezaba a correr automáticamente, lo cual no tenía sentido si el cliente todavía no había hecho la acción principal. `pedido_reinicio_controller.js` ya no auto-arranca en `connect()`; expone una acción `iniciar()` enganchada al click del link de WhatsApp (que igual abre en pestaña nueva, así que no interrumpe el timer). El texto "Esta página se reiniciará en 60s" también arranca oculto y solo aparece una vez que se hizo click.
     Verificado con la suite completa (146 tests, sigue en verde), sintaxis de los 2 controllers JS nuevos/tocados validada, y el HTML renderizado contra el server real por `curl` confirmando que el contenido colapsable arranca oculto (`hidden`) y que el aviso de reinicio también arranca oculto hasta el click. **No se pudo confirmar visualmente el toggle ni la cuenta regresiva funcionando en un browser real** (mismo motivo de siempre) — falta que Joaquín lo pruebe a mano.
  5. [x] Mi perfil (2026-08-25) — ver el rediseño completo (panel/secciones, botones, tipografía) más abajo en "Todo lo que falta antes de eso", quedó documentado ahí en detalle por lo largo del cambio.
  6. [x] Nuestros productos (catálogo + ficha de cada producto) (2026-08-24/2026-08-25) — catálogo rediseñado el 2026-08-24 (barra de filtros, 4 columnas, buscador); ficha de producto ajustada el 2026-08-25 (colores a navy, 3 secciones con el botón alineado al borde de la foto). Detalle más abajo.
  7. [x] Consultar pedido (2026-08-25) — revisado contra el mismo patrón (`.page-header`/`.page-header-desc`, igual que "Mi perfil"): ya cumplía, sin cambios de estilo necesarios. La pantalla de resultado (`pedidos_publicos#show`) es la misma que "Pedido confirmado" (ítem 4), ya cubierta ahí.
  8. [x] Admin: agregar filtro para seleccionar pedidos por fecha en la vista de Pedidos (2026-08-25) — ver detalle más abajo, junto con la limpieza de estilos inline del resto del panel admin.
- [ ] **Fotos reales (contenido real del negocio)** (unificado 2026-08-25 — antes eran 3 ítems separados) — todo lo que hoy es un placeholder y falta reemplazar por fotos profesionales cuando Joaquín las tenga:
  - Los 2 productos de prueba (`public/docs/productos/`) — subir el archivo, commitear, deployar y cargar el nombre exacto en cada producto desde el admin. (Las descripciones de texto ya están escritas — ver `db/seeds.rb`.)
  - Hero de la Home (`public/docs/banners/home-hero.jpg`).
  - "Quiénes somos" — misma foto (`public/docs/banners/quienes-somos.jpg`) reutilizada en la Home y en `/nosotros`; idealmente una foto de los 3 amigos/el equipo, dado el copy nuevo.
  - "Nuestro proceso" en `/nosotros` (`public/docs/banners/nuestro-proceso.jpg`).
  Mismo mecanismo en los 4 casos (asset estático versionado en el repo, sin backend externo) — no hay nada pendiente del lado del código, solo falta que existan las fotos.
- [ ] Variantes/miniaturas de fotos de producto (hoy se sirve la imagen original sin redimensionar; no bloqueante, solo rendimiento)
- [ ] **Contenido real en las páginas de texto** (2026-08-17, "Nosotros" resuelto 2026-08-18): "Nuestros productos" y páginas similares pueden tener todavía texto de relleno pendiente de revisar — falta que Joaquín confirme si queda algo por reemplazar antes de salir a producción. (La sección "Nuestra historia" de `/nosotros`, que era el caso más obvio, ya tiene contenido real — ver ítem 1 del repaso view-por-view arriba.)
- [ ] Confirmar visualmente en un browser real el rediseño completo de la Home (paleta, nav flotante/transparente, hero, collage de productos, "Quiénes somos") — verificado solo por HTML/CSS/tests, sin browser disponible en este entorno.
- [ ] **Responsive / versión mobile** (2026-08-17): todo el sitio está pensado y probado en pantalla de PC — falta revisar y ajustar cómo se ve en el teléfono (nav, hero, collage de productos, "Quiénes somos", footer, catálogo con filtros). Se soluciona con media queries en `app/assets/stylesheets/application.css` adaptando los layouts de grid/flex actuales a una sola columna en pantallas angostas. Dejado a propósito como el penúltimo paso: se hace cuando el resto del sitio (contenido, fotos, textos) ya esté terminado y confirmado en PC, justo antes de sacar el sitio del modo privado.
- [x] **Banners con foto de "Quiénes somos" y "Nuestros productos" eliminados** (2026-08-24, a pedido de Joaquín) — resuelve la duda pendiente del ítem anterior: se sacaron los banners (`.catalogo-hero`, `.nosotros-hero`, con foto placeholder de fondo, breadcrumb y bajada) de ambas páginas. Cada página ahora arranca directo en su contenido, sin ningún título arriba. CSS muerto de ambos banners eliminado de `application.css` (incluida la regla mobile de `.catalogo-hero`). Las fotos de las secciones "Sobre nosotros"/"Nuestro proceso" (más abajo en `/nosotros`) no se tocaron, solo el banner de arriba. `public/docs/banners/catalogo-hero.jpg` quedó sin usar (no se borró el archivo).
  **Ajuste inmediato (2026-08-24, a pedido de Joaquín)**: se había dejado un `<h1>` plano ("Nuestros productos" / "Quiénes somos") reemplazando al banner, pero Joaquín pidió sacar también ese título — ninguna de las dos páginas tiene ahora un `<h1>` visible (en "Quiénes somos" quedan los `<h2>` "Sobre nosotros"/"Nuestro proceso"; el catálogo arranca directo en filtros/grilla). **Nota de accesibilidad**: esto deja ambas páginas sin un `<h1>` (WCAG 4.1.1/estructura de encabezados, no es un error crítico pero rompe la jerarquía semántica que se armó en la auditoría AA de 2026-08-17) — si en algún momento se quiere retomar, lo más simple es un `<h1>` visualmente oculto (`.sr-only`, clase que ya existe en el sitio) que no cambie nada visualmente.
  Verificado con la suite completa (146 tests, 458 assertions, sigue en verde) y el HTML renderizado contra el server real por `curl`, confirmando que no queda ningún `<h1>` en ninguna de las dos páginas y que no quedan referencias a las clases de banner eliminadas.

- [x] **Catálogo: barra de filtros horizontal en vez de sidebar** (2026-08-24, a pedido de Joaquín). Antes era un layout de 2 columnas (`.catalogo-layout`: sidebar de filtros de 220px a la izquierda + grilla a la derecha). Ahora es una sola columna: barra de filtros horizontal (`.catalogo-filtros-barra`) arriba, a todo el ancho de `.catalogo-page` (1600px), y la grilla de productos debajo ocupando también todo ese ancho — con más espacio disponible, `.productos-grid` (que ya usaba `grid-template-columns: repeat(auto-fill, minmax(220px, 1fr))`, sin cambios) acomoda más columnas por fila automáticamente, sin necesidad de tocar esa regla.
  - **Filtros de precio y categoría, de radio buttons a `<select>`**: en una fila horizontal, un grupo de 6 radio buttons (precio) no entraba de forma prolija — se convirtieron a `f.select` (mismo patrón que ya usaba "Ordenar por"), mismos `name` de parámetro (`precio_rango`, `categoria`) y mismo `data-action="change->filtros#aplicar"` para auto-enviar el formulario al cambiar — **la lógica de filtrado en el controller no se tocó**, sigue leyendo los mismos params de la misma forma. El campo "Buscar" se mantiene como texto libre (sigue sin auto-enviar, requiere el botón "Aplicar filtros", igual que antes).
  - **Responsive**: en pantallas ≤768px aparece un botón "Filtros" (con ícono de flecha que rota, mismo patrón visual que "Resumen de tu pedido" en la pantalla de pedido confirmado) que colapsa/expande los campos — reutiliza el controller Stimulus genérico `colapsable_controller.js` que ya existía (sin JS nuevo), combinado en el mismo `<form>` con el controller `filtros` ya existente (`data-controller="filtros colapsable"`, dos controllers en un mismo elemento). Los campos arrancan visibles por defecto en todos los tamaños de pantalla — la barra completa es funcional sin tocar el botón, que solo sirve para achicar visualmente el bloque en mobile si se quiere. En pantallas anchas el botón queda oculto vía CSS y los campos se muestran siempre en fila; en mobile pasan a columna con separación.
  - Colores/estilo: se mantuvo el sistema de diseño ya existente del sitio (`--navy` #081f2e / `--ocean` #145577 / `--celeste` #5fbad9, no el hex que se sugirió en el pedido, que no correspondía a ninguna variable real del CSS del sitio) — mismo `.btn-ocean` para "Aplicar filtros", mismo `--border`/`--navy` para bordes e ícono del toggle.
  - CSS muerto eliminado: `.catalogo-layout`, `.catalogo-filtros`, `.filtros-titulo`, `.filtro-grupo`, `.filtro-check` (y su regla mobile `.catalogo-layout { grid-template-columns: 1fr }`) — ya no se usan, reemplazados por `.catalogo-filtros-barra`/`.catalogo-filtros-campos`/`.filtro-campo`.
  - **Archivos modificados**: `app/views/productos/index.html.erb` (estructura del formulario de filtros, radios → selects) y `app/assets/stylesheets/application.css` (nuevas reglas de la barra horizontal + su media query mobile, CSS viejo del sidebar eliminado). No se tocó `app/controllers/productos_controller.rb`, `app/javascript/controllers/filtros_controller.js` ni `app/javascript/controllers/colapsable_controller.js` — se reutilizaron tal cual.
  - Verificado con la suite completa (146 tests, 458 assertions, sigue en verde) y contra el server real por `curl`: los 3 selects (orden/precio/categoría) usan los mismos `name` de siempre, `precio_rango=0-4990` sigue filtrando correctamente y el `<option selected>` queda bien marcado según el filtro activo en la URL. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre, sin Chromium en este entorno) — falta que Joaquín confirme que la barra se ve prolija en desktop y que el botón "Filtros" colapsa bien en mobile.

- [x] **Catálogo: fotos más grandes (4 columnas fijas), ancho igual a la Home, filtro de categoría eliminado** (2026-08-24, a pedido de Joaquín).
  - **Grilla a 4 columnas fijas**: `.productos-grid` pasó de `grid-template-columns: repeat(auto-fill, minmax(220px, 1fr))` (que en un contenedor ancho podía mostrar 5-6 columnas chicas) a `repeat(4, 1fr)` — siempre 4 por fila en desktop, cards y fotos más grandes (la foto ya escalaba al 100% del ancho de la card vía `aspect-ratio: 1/1`, así que no hizo falta tocar `.producto-foto`). En mobile (≤768px) pasa a `repeat(2, 1fr)`.
  - **Filtro de categoría eliminado**: se sacó el `<select>` de "Categoría" de la barra. **Se sacó también el campo "Buscar"** — el pedido decía literalmente "solo deja ordenar por y precio", así que quedaron solo esos dos filtros más el botón "Aplicar filtros" (avisar si el buscador se quería mantener, se puede volver a agregar). El controller (`ProductosController`) no se tocó — sigue soportando `buscar`/`categoria` como parámetros de URL aunque ya no haya UI para ellos, por si se quiere reintroducir alguno más adelante sin tocar el backend.
  - **Ancho de catálogo igual al de la Home (960px, no 1600px)**: `.catalogo-page` dejó de tener `max-width: 1600px` (agregado el 2026-08-05) y pasó al mismo patrón de ancho que ya usa la Home (`.seccion`/`.seccion-inner`, `.hero`/`.hero-inner`): un wrapper con padding horizontal (`.catalogo-page`, `padding: 2.5rem 2rem`) + un contenido interno con `max-width: 960px; margin: 0 auto` (`.catalogo-inner`, nuevo) — la barra de filtros y la grilla de productos ahora comparten ese mismo ancho de 960px, así que quedan alineadas entre sí (tercer pedido del punto 3) automáticamente, sin CSS adicional.
  - **Bug real encontrado y corregido en "Quiénes somos"**: al intentar igualar su ancho al de la Home, se encontró que `app/views/pages/nosotros.html.erb` nunca seteaba `content_for :fluid, true` (a diferencia de `pages/inicio.html.erb` y `productos/index.html.erb`, que sí lo hacen) — el layout (`application.html.erb`) envuelve automáticamente cualquier página sin ese flag en su propio `.container` (960px, padding 1.5rem). Como resultado, **toda la página de "Quiénes somos" quedaba doblemente contenida**: el `.container` genérico del layout por fuera, y el contenido de cada sección por dentro — la banda gris de "Nuestro proceso", que el README ya documentaba como "banda a todo el ancho de la pantalla" (2026-08-19), en realidad nunca llegaba a los bordes de la ventana, quedaba encogida dentro del `.container` de 960px. Se agregó `content_for :fluid, true` a `nosotros.html.erb` (mismo patrón que las otras dos páginas) y se movió el ancho/padding a las clases propias de la página: `.nosotros-seccion` ahora tiene `padding: 3.5rem 2rem` (antes `3.5rem 0`, dependía del `.container` externo para el padding horizontal) y `.nosotros-seccion-grid` tiene `max-width: 960px; margin: 0 auto` (antes solo la variante `--gris` lo tenía, de forma redundante) — mismo patrón exacto que `.seccion`/`.seccion-inner` de la Home. Con esto la banda gris de "Nuestro proceso" ahora sí llega a los bordes de la pantalla, y "Sobre nosotros" queda al mismo ancho de contenido (960px) que el resto del sitio.
  - **Archivos modificados**: `app/views/productos/index.html.erb` (se sacaron categoría/buscar, se agregó `.catalogo-inner`), `app/views/pages/nosotros.html.erb` (se agregó `content_for :fluid`, se sacó el `<div class="container">` explícito que ya no hacía falta), y `app/assets/stylesheets/application.css` (`.productos-grid` a 4/2 columnas, `.catalogo-page`/`.catalogo-inner` nuevos, `.nosotros-seccion`/`.nosotros-seccion-grid`/`.nosotros-seccion--gris` ajustados al patrón de ancho de la Home, CSS muerto de `.filtro-campo--buscar`/`.filtro-buscar` eliminado).
  - Verificado con la suite completa (146 tests, 458 assertions, sigue en verde) y contra el server real por `curl`: el catálogo ya no trae los campos de categoría/buscar (solo `orden`/`precio_rango`), el filtro de precio sigue funcionando, y `/nosotros` ya no queda envuelto en el `.container` del layout. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que las 4 columnas de fotos y el ancho compartido con la Home se ven bien, y que la banda gris de "Nuestro proceso" ahora sí llega a los bordes de la pantalla.
  **Ajuste (2026-08-24, a pedido de Joaquín)**: ronda de correcciones sobre el punto anterior.
  - **Ancho +30% en Home, Nuestros productos y Quiénes somos**: nueva variable `--ancho-contenido: 1248px` en `:root` (960px + 30%), usada en los 5 contenedores de contenido de esas 3 páginas — `.hero-inner`, `.seccion-inner` y `.quienes-somos-grid` (Home), `.catalogo-inner` (catálogo), `.nosotros-seccion-grid` (Quiénes somos). El resto del sitio (`.container`, usado por checkout/carrito/perfil/admin/devise/FAQ, y `.footer-inner`, global) se dejó en 960px a propósito — no se pidió ensancharlos y cambiar `.container` habría afectado páginas fuera del pedido.
  - **Buscar de vuelta en el catálogo**: se había sacado en el ajuste anterior interpretando "solo deja ordenar por y precio" de forma literal — Joaquín aclaró que sí lo quería de vuelta. Reincorporado como primer campo de la barra (antes de "Ordenar por"), con el mismo `data-action="change->filtros#aplicar"` que ya usan los selects, así también auto-aplica (al perder foco o con Enter) en vez de depender de un botón.
  - **Botón "Aplicar filtros" eliminado**: ya no hacía falta — los 3 campos (buscar/orden/precio) auto-aplican por `change`. Se sacó también el CSS asociado (`.catalogo-filtros-aplicar`).
  - **Fotos ~30% más grandes**: la grilla de productos se mantuvo en 4 columnas fijas (pedido anterior), así que al ensanchar el contenedor un 30% cada card/foto también creció proporcionalmente (~32% en la práctica, con 4 productos de prueba) sin tocar `.producto-foto` — la foto ya escalaba al 100% de su card. La foto de "Quiénes somos" en la Home (`.quienes-somos-foto`, que tenía un `max-width: 380px` fijo en vez de depender del ancho de su columna) sí necesitó un ajuste explícito para no quedar chica dentro de una columna más ancha — se subió a `494px` (380 + 30%), misma proporción 4:5. Las fotos de `/nosotros` ("Sobre nosotros"/"Nuestro proceso") no tienen `max-width` fijo, así que ya crecían solas con el resto de la sección, sin necesitar cambios.
  - **Revisión de que nada se desordenó**: el stepper/botón "Agregar al carrito" superpuesto sobre la foto (`.producto-accion`) usa posicionamiento relativo a su contenedor (`left/right/bottom: 1rem`, sin píxeles fijos que asumieran el tamaño viejo de la card), así que escaló bien con la foto más grande — probado de punta a punta contra el server real: `POST /carrito/agregar` desde una card del catálogo devuelve el turbo-stream esperado (reemplaza el botón por el stepper, actualiza el badge del carrito en el nav). Los tamaños de letra (nombre/precio de producto, textos de "Quiénes somos", etc.) no se tocaron — son unidades `rem` independientes del ancho del contenedor, así que no cambiaron.
  - Verificado con la suite completa (146 tests, 458 assertions, sigue en verde) y contra el server real por `curl`: el campo "Buscar" volvió a filtrar correctamente (`?buscar=pate`), ya no aparece el botón "Aplicar filtros" en el HTML, y los 5 contenedores de ancho usan la nueva variable de 1248px. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre, sin Chromium en este entorno) — falta que Joaquín confirme que el ancho nuevo y las fotos más grandes se ven bien y nada quedó desalineado.
  **Fix: foto de "Quiénes somos" recortada (2026-08-24, reportado por Joaquín)**: causa raíz encontrada — `public/docs/banners/quienes-somos.jpg` (el placeholder, copia de una foto de producto) es una imagen vertical angosta (720×1280 px, proporción ~9:16), pero el marco CSS de la foto es casi cuadrado (`aspect-ratio: 4/5`). Con `object-fit: cover` (el valor de siempre) el navegador recorta lo que sobra para llenar el marco — con esta combinación de proporciones, recortaba **~30% de la imagen, arriba y abajo** — y al agrandar el marco un 30% en el ajuste anterior, ese recorte se hizo mucho más notorio. Mismo problema en las dos fotos de `/nosotros` (`quienes-somos.jpg` reutilizada, y `nuestro-proceso.jpg`, mismas dimensiones). Cambiado `object-fit: cover` → `object-fit: contain` en `.quienes-somos-foto img` (Home) y `.nosotros-seccion-foto img` (`/nosotros`) — con `contain` la imagen nunca se recorta, sea cual sea su proporción; con las fotos placeholder actuales queda con una franja de fondo (`var(--foam)`, agregado como `background` al contenedor) a los costados en vez de perder contenido arriba/abajo. **Este fix es robusto para cualquier foto futura** — cuando se reemplacen los placeholders por fotos reales (pendiente en la lista de abajo), si vienen en una proporción parecida al marco (4:5) no se van a ver franjas y quedará exactamente igual que antes; si vienen en otra proporción, en el peor caso quedan con una franja de fondo en vez de perder parte de la foto, que es preferible a cortar contenido sin querer.
  Verificado con la suite completa (146 tests, sigue en verde) y el CSS revisado a mano contra el server real confirmando `object-fit: contain` en ambos selectores. **No se pudo confirmar visualmente en un browser real** — falta que Joaquín confirme que ya no se ve cortada.

- [x] **Catálogo: más opciones de orden, tipografía de filtros unificada, buscador con lupa** (2026-08-24, a pedido de Joaquín).
  - **Más opciones en "Ordenar por"**: antes solo Posición/Precio asc/Precio desc/Nombre. Se agregaron "Nombre: Z-A" (antes solo A-Z) y "Más nuevos primero" (por fecha de creación). `ProductosController::ORDENES` pasó de 3 a 5 claves (`precio_asc`, `precio_desc`, `nombre_asc` — renombrada desde `nombre` —, `nombre_desc`, `recientes`); es un hash interno del controller, no una API externa, así que renombrar la clave no rompe nada.
  - **Bug real encontrado y corregido — tipografía inconsistente entre filtros**: el campo "Buscar" se veía distinto a los `<select>` de "Ordenar por"/"Precio" porque su regla CSS (`.filtro-buscar`, una sola clase) tenía **menos especificidad** que la regla global `input[type="text"]` de la sección FORMULARIOS del mismo archivo — el input de búsqueda terminaba heredando el tamaño de letra, padding y color de borde genéricos del sitio (0.95rem, `#ccc`) en vez de los del catálogo (0.88rem, `var(--border)`), aunque el CSS "correcto" estaba escrito, simplemente perdía la pulseada de especificidad. Se resolvió con un selector con más peso (`.filtro-campo input[type="text"]`, igual que ya se usaba para los `<select>`) que gana de forma predecible sin depender del orden de las reglas en el archivo — ahora los 3 campos (buscar/orden/precio) comparten exactamente el mismo `font-size`, `padding`, `border` y `height` (2.75rem, mismo alto que ya tenían los `<select>`).
  - **Lupa en el campo de búsqueda**: ícono de lupa (mismo estilo de línea SVG que el resto del sitio) al extremo derecho del input, dentro de un `<button type="submit">` — hace de botón de búsqueda real (envía el formulario al hacer click), no solo un ícono decorativo. Como ahora hay una acción explícita para buscar, se sacó el auto-envío `change->filtros#aplicar` que tenía el campo de texto (auto-enviaba al perder el foco) — ahora se busca con la lupa o con Enter, igual que un buscador convencional; "Ordenar por" y "Precio" mantienen su auto-envío al cambiar, sin acción explícita necesaria ahí.
  - **Placeholder del buscador cambiado**: decía "Nombre o descripción..." — Joaquín pidió sacar ese texto. Se reemplazó por "Buscar productos..." (el detalle de que busca por nombre o descripción se mantiene solo en el `aria-label`, para lectores de pantalla, no como texto visible).
  - Verificado con la suite completa (146 tests, 458 assertions, sigue en verde), Rubocop limpio sobre el controller, y contra el server real por `curl`: las 2 opciones de orden nuevas (`orden=nombre_desc`, `orden=recientes`) devuelven 200, el placeholder ya no dice "Nombre o descripción...", y la lupa (`<button type="submit">`) está presente dentro del campo. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que los 3 filtros se ven con la misma letra ahora y que la lupa se ve/funciona bien.

- [x] **Revertido el fix de `object-fit: contain` en las fotos de "Quiénes somos" (Home y `/nosotros`) + balance de altura + copy nuevo** (2026-08-24, a pedido de Joaquín/ticket). El fix anterior (`object-fit: contain` para no recortar la foto placeholder vertical) tuvo un efecto secundario que se reportó como "bordes blancos raros" / "no quedó ajustada al espacio": al no coincidir la proporción de la foto (vertical angosta) con el marco (4:5, casi cuadrado), `contain` dejaba franjas del color de fondo (`--foam`) a los costados de la imagen en vez de llenarlo. Se volvió a `object-fit: cover` (sin franjas, llena todo el marco) en los 3 lugares que se habían tocado: `.quienes-somos-foto img` (Home), `.nosotros-seccion-foto img` (`/nosotros`, ambas secciones) — y se sacó el `background: var(--foam)` que ya no cumplía ninguna función. **Esto reintroduce el recorte de ~30% de la foto** que motivó el cambio a `contain` en primer lugar (documentado más arriba) — es el trade-off explícito que se pidió esta vez: mejor una foto que llene el marco (aunque recorte extremos) que una con franjas de fondo alrededor. Sigue siendo un problema exclusivamente del placeholder (foto de producto reciclada, proporción equivocada para un marco de foto de equipo) — se resuelve solo con una foto real bien encuadrada para este uso.
  - **Balance de altura en "Sobre nosotros"/"Nuestro proceso"**: la foto (marco fijo 4:5) quedaba bastante más alta que el bloque de texto (2 párrafos), dejando un espacio en blanco grande debajo del texto porque `.nosotros-seccion-grid` alineaba ambas columnas arriba (`align-items: start`, decisión de una ronda anterior para que el título quedara a la altura del borde superior de la foto). Se cambió a `align-items: center` — el texto ahora queda centrado verticalmente contra la foto, repartiendo el espacio sobrante arriba y abajo en vez de dejarlo todo abajo. Esto reemplaza la alineación "arriba" pedida antes; si se prefiere volver a eso más adelante, la alternativa (probada y descartada acá) es forzar la altura de la foto a la del texto en vez de centrar.
  - **Copy actualizado en ambas secciones de `/nosotros`** (texto exacto provisto): "Sobre nosotros" ahora cuenta que lo que partió como gusto entre amigos se transformó en Fuyén, con receta propia perfeccionada durante años; "Nuestro proceso" ahora menciona ahumado en caliente, sin conservantes ni atajos, un proceso de salado/ahumado/enfriado de más de un día, y que el resultado es un producto artesanal en pequeños lotes.
  - **Archivos modificados**: `app/views/pages/nosotros.html.erb` (copy nuevo en los 2 párrafos de cada sección) y `app/assets/stylesheets/application.css` (`.quienes-somos-foto`/`.quienes-somos-foto img`, `.nosotros-seccion-grid`, `.nosotros-seccion-foto`/`.nosotros-seccion-foto img`). No se tocó `app/views/pages/inicio.html.erb` — solo CSS de la Home, sin cambios de estructura HTML ahí.
  - Verificado con la suite completa (146 tests, 458 assertions, sigue en verde) y contra el server real por `curl`: el copy nuevo aparece completo en el HTML de `/nosotros`, y las 3 reglas `object-fit` en cuestión quedaron en `cover`. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que ya no se ven franjas blancas y que el balance vertical entre foto y texto se ve bien.
  **Ajuste (2026-08-24, a pedido de Joaquín)**: pidió específicamente que la parte de arriba de la foto calzara con el título ("Sobre nosotros"/"Nuestro proceso") en vez de quedar centrada — se volvió a `align-items: start` en `.nosotros-seccion-grid` (el valor de antes del ajuste de "balance de altura" de este mismo punto, unas líneas arriba). Vuelve a existir espacio en blanco debajo del texto cuando la foto es más alta que el párrafo (el trade-off que ese ajuste intentaba evitar) — es intencional, a pedido explícito de Joaquín; si en algún momento se quiere evitar ese espacio sin perder el alineado de arriba, la alternativa real es ajustar la altura/proporción de la foto para que se acerque más a la del texto, no jugar con `align-items`. Verificado con la suite completa (146 tests, sigue en verde) y el CSS confirmado contra el server real.
- [x] **Ronda de ajustes finos en Home/Quiénes somos + párrafos justificados en todo el sitio** (2026-08-24, a pedido de Joaquín).
  - **Botón "Conoce nuestra historia" a la altura del borde inferior de la foto (Home)**: antes el botón quedaba pegado al final del párrafo, en cualquier altura intermedia — se veía descuadrado contra la foto (más alta). `.quienes-somos-texto` pasó a `align-self: stretch` (se estira a la altura completa de la fila, que la foto define por ser más alta) y el botón (`.quienes-somos-texto > .btn-navy-grande`) a `margin-top: auto` — truco estándar de flexbox para empujar el último elemento al fondo del contenedor sin tocar el resto (título/párrafos siguen agrupados arriba, sin separarse entre sí).
  - **Más espacio arriba en "Quiénes somos"**: `.nosotros-seccion:first-of-type` tenía `padding-top: 0` (pegado al nav) — se subió a `2.5rem`, mismo aire que `.container` le da al resto de las páginas del sitio entre el nav fijo y el contenido.
  - **Línea separadora negra entre "Sobre nosotros" y "Nuestro proceso"**: la página no tenía ninguna línea entre ambas secciones (`.nosotros-seccion` usaba `border-top: 1px solid var(--border)` gris-celeste, pero tanto la primera sección como la banda gris de "Nuestro proceso" la tenían explícitamente desactivada con `border-top: none`). Se cambió el color base a `#000` (igual que `.seccion` en la Home) y se sacó el `border-top: none` de `.nosotros-seccion--gris` — ahora se ve la misma línea negra fina que separa las secciones de la Home.
  - **Párrafos justificados en todo el sitio**: nueva regla `p { text-align: justify; }` en la sección de tipografía global. Al ser una regla directa sobre `p` (no solo heredada), se detectaron y corrigieron **4 casos reales** donde un `<p>` dependía de un `text-align: center` heredado de su contenedor — con la regla nueva, una regla que aplica directo sobre el elemento gana siempre contra un valor solo heredado, así que estos 4 se habrían desalineado silenciosamente sin el fix: `.hero-subtitulo` (bajada del hero en la Home), `.page-header-desc` (bajada de encabezados tipo "Consultar mi pedido"), `.whatsapp-cta p` (texto dentro del cuadro verde de WhatsApp) y el `<p>` de copyright en `.footer-copy`. A cada uno se le agregó `text-align: center` explícito para que seguros centrados. Los textos ya centrados por una clase propia con más especificidad que un simple `p` (ej. `.auth-alt`, `.producto-precio`, `.carrito-item-subtotal`) no se vieron afectados, no necesitaron cambios.
  - **Nota de legibilidad**: texto justificado sin guionado (`hyphens`) puede dejar espacios más irregulares entre palabras en columnas angostas (mobile) — no se agregó `hyphens: auto` porque no se pidió; avisar si en algún momento se ve mal en el celular y se quiere ese ajuste extra.
  - **Archivos modificados**: solo `app/assets/stylesheets/application.css` — ningún cambio de HTML/vistas en este punto.
  - Verificado con la suite completa (146 tests, 458 assertions, sigue en verde) y contra el server real por `curl` (Home y `/nosotros` cargan 200, las reglas nuevas confirmadas en el CSS servido). **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme los 4 ajustes de layout y que la justificación de párrafos se vea bien en las páginas con más texto (Quiénes somos, FAQ).
- [ ] **Mejorar el PDF del "comprobante" (descarga del pedido) y el Excel de despacho del admin** (2026-08-19, a pedido de Joaquín) — sin especificar todavía en qué sentido mejorarlos (diseño, contenido, ambos); falta que Joaquín aclare qué le gustaría cambiar de cada uno.
- [x] **Carrito vacío con ícono y CTA en vez de texto plano** (2026-08-25, a pedido de Joaquín). Antes el carrito vacío era solo `<p>Tu carrito está vacío.</p>` + botón "Ver productos" sin ningún estilo, sin coherencia visual con el resto del sitio. Ahora es un estado vacío real (`.carrito-vacio`): mismo ícono SVG de carrito que ya usa el nav (`shared/_nav_carrito.html.erb`, reutilizado tal cual para no crear un ícono nuevo) en gris/`var(--border)` a 56px, texto centrado y el botón "Ver productos" (pasó de `btn-primary` a `btn-ocean`, mismo color que el resto de los CTA de esta página). Cambio simple a propósito, sin productos sugeridos ni nada adicional (a pedido explícito de Joaquín). Archivos: `app/views/carrito/_contenido.html.erb`, `app/assets/stylesheets/application.css`.
  **Ajuste (2026-08-25, tras verlo Joaquín en el navegador)**: botón, ícono y texto poco visibles en la primera versión. Botón vuelto a `btn-primary` (navy, como el resto del sitio antes de este cambio). Ícono pasó de `var(--border)` (#d5e3ea, un celeste muy pálido pensado para bordes sutiles, no para un ícono independiente) a `var(--ocean)` (#145577, mismo azul de acento que ya usa el resto del sitio). Texto pasó de `#555`/1.2rem a `var(--ink)` (#1c2b35, el color de texto principal del sitio)/1.3rem/600 — más peso y contraste para que no se pierda en el espacio vacío alrededor.
  Verificado con la suite completa (146 tests, 458 assertions, sigue en verde) y contra el server real por `curl` confirmando el HTML/CSS servidos.
- [x] **Rediseño de "Mi perfil": panel gris con bordes verticales, 4 secciones distinguidas (nueva "Mis reseñas") y títulos unificados** (2026-08-25, a pedido de Joaquín).
  - **Se sacó el título de la página** ("Mi perfil" + bajada "Tus datos y el historial de tus pedidos") — ya no queda ningún encabezado genérico arriba, entra directo en las secciones.
  - **Panel central gris con dos líneas verticales** (`.perfil-panel`, nuevo): envuelve todo el contenido de la página con fondo `#eaedf0` (mismo gris que ya usa el sitio en "Quiénes somos"/"Nuestro proceso") y `border-left`/`border-right` de 1px — separa visualmente la zona de contenido del fondo blanco general de la página (que sigue blanco tanto fuera de `.container` como en su padding).
  - **Las 4 secciones ahora se distinguen entre sí**: "Mis datos" y "Mis pedidos" quedan en una tarjeta blanca (`.perfil-card`, reusa el patrón `.admin-card`/`.direccion-card` ya existente) apoyada sobre el panel gris; "Mis direcciones" mantiene sus tarjetas individuales por dirección (ya eran blancas); se agregó una **cuarta sección nueva, "Mis reseñas"**, también en tarjeta blanca, listando las reseñas que el cliente dejó en cualquier producto (nombre del producto con link a su ficha, estrellas, fecha y comentario si escribió uno — reutiliza `shared/_estrellas` y el mismo patrón visual `.resena-item` que ya usaba la ficha de producto) con mensaje `.empty-state` si todavía no dejó ninguna. Controller: `PerfilController#show` ahora también carga `@resenas = current_user.resenas.includes(:product).order(created_at: :desc)`.
  - **Los 4 títulos de sección ahora usan el mismo formato de título que el resto del sitio** (Fraunces 1.8rem/600/navy, clase nueva `.perfil-seccion-titulo` aplicada a un `<h2>` — mismo criterio que ya se usó en "Resumen de tu pedido" del checkout para no tocar el `h2` genérico del resto del sitio) — antes "Mis datos" usaba el estilo de eyebrow chico de `.admin-card h2` (0.85rem mayúsculas) y "Mis direcciones"/"Mis pedidos" el `h2` genérico (1.3rem sin color), inconsistentes entre sí y con el resto del sitio. El texto de cuerpo dentro de las tarjetas (`dl`/`dd` de "Mis datos", celdas de la tabla de "Mis pedidos", comentario de reseña) subió de tamaños dispares (0.8rem–0.95rem) a un tamaño de cuerpo uniforme (1.05rem) dentro de `.perfil-card`.
  - **Nota de accesibilidad**: la página queda sin ningún `<h1>` (mismo trade-off ya aceptado y documentado en el rediseño de banners de "Quiénes somos"/catálogo) — si se quiere retomar más adelante, un `<h1 class="sr-only">Mi perfil</h1>` no visible resolvería la jerarquía sin cambiar nada del diseño.
  - **Archivos modificados**: `app/views/perfil/show.html.erb` (reescrita), `app/controllers/perfil_controller.rb`, `app/assets/stylesheets/application.css` (sección nueva "PERFIL"). No se tocó `perfil/edit.html.erb` ni ninguna otra vista.
  - Verificado con la suite completa (146 tests, 458 assertions, sigue en verde), Rubocop limpio sobre el controller, y contra el server real logueado como el cliente de prueba por `curl`: las 4 secciones aparecen con sus títulos, el panel (`.perfil-panel`) envuelve todo el contenido, y la sección de reseñas muestra la reseña real que el cliente de prueba ya tenía cargada. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que el panel gris con las líneas verticales y las 4 tarjetas se ven como se pidió.
  **Ronda de ajustes (2026-08-25, tras verlo Joaquín, varios pedidos en una sola instrucción)**:
  - **Se sacó el fondo gris + líneas verticales de todo el panel**, reemplazado por **líneas horizontales negras** que separan las 4 secciones entre sí — mismo patrón exacto que ya usa `/nosotros` para separar "Sobre nosotros" de "Nuestro proceso" (`border-top: 1px solid #000` en `.perfil-seccion`, sin borde en la primera sección). El fondo blanco general de la página volvió a ser el de siempre.
  - **Solo 2 de las 4 secciones mantienen fondo gris**: "Mis direcciones" y "Mis reseñas" (`#eaedf0`, mismo gris de siempre), ahora como una caja redondeada (`.perfil-seccion-gris`, `border-radius: 8px`) que envuelve el título + contenido de esa sección únicamente — "Mis datos" y "Mis pedidos" quedaron sin fondo, directo sobre el blanco de la página.
  - **Un solo botón "Editar Datos" que incluye email y contraseña**: se sacó el botón separado "Cambiar email o contraseña" — el botón único de "Mis datos" ahora apunta a `/users/edit` (la pantalla de Devise), a la que se le agregaron los campos de nombre/apellido/teléfono (que ya venían en `perfil/edit.html.erb`) arriba de email/contraseña. Como consecuencia:
    - **`PerfilController#edit`/`#update` y la vista `perfil/edit.html.erb` se eliminaron** (quedaron sin uso) — el ApplicationController ya permitía `nombre`/`apellido`/`telefono` en el sanitizer de Devise (`configure_permitted_parameters`, agregado en una sesión anterior), así que no hizo falta tocar esa parte.
    - **Ahora cualquier cambio de datos (incluido nombre/teléfono) pide la contraseña actual** — antes solo se pedía para cambiar email/contraseña; es el comportamiento estándar de `update_with_password` de Devise, y es intencional (un solo formulario de cuenta, con la misma confirmación de seguridad para todo).
    - **Nuevo `Users::RegistrationsController`** (`app/controllers/users/registrations_controller.rb`, registrado en `routes.rb` vía `devise_for :users, controllers: { registrations: "users/registrations" }`) que sobreescribe `after_update_path_for` para volver a `/perfil` en vez del `root_path` por defecto de Devise — evita la regresión de que guardar tu teléfono te mande a la Home en vez de volver a tu perfil.
    - **2 tests de `perfil_direcciones_test.rb` actualizados** (apuntaban al endpoint viejo, `patch perfil_path`) para pegarle al nuevo flujo (`patch user_registration_path`, con `current_password`), más un test nuevo confirmando que el cambio se rechaza sin la contraseña actual.
  - **Botón de "Nueva dirección" bajó de la cabecera al final de la lista de direcciones** (debajo de todas las direcciones ya cargadas, no arriba a la derecha del título).
  - **Botones "Editar datos" y "Nueva dirección" más grandes**: pasaron de `btn-primary btn-sm` a `btn-navy-grande` (la clase ya existente para CTAs grandes del sitio, la misma que usa "Conoce nuestra historia" en la Home) — más anchos y altos, sin inventar un estilo nuevo. Se sacó el "+" de "Nueva dirección" (queda solo el texto).
  - **Texto de cuerpo unificado al tamaño de "Quiénes somos"**: los valores de "Mis datos" (`dd`), las celdas de la tabla de "Mis pedidos", el detalle de cada dirección y el comentario de cada reseña pasaron de tamaños dispares (0.8rem–0.95rem, herencia de estilos de admin) a **1.2rem / `#555` / line-height 1.8** — exactamente `.quienes-somos-texto .seccion-texto`, la clase que define el cuerpo de la sección "Quiénes somos" de la Home.
  - **Archivos**: `app/views/perfil/show.html.erb`, `app/views/devise/registrations/edit.html.erb`, `app/assets/stylesheets/application.css`, `app/controllers/perfil_controller.rb` (simplificado a solo `show`), `app/controllers/users/registrations_controller.rb` (nuevo), `config/routes.rb`, `test/integration/perfil_direcciones_test.rb`. Se eliminó `app/views/perfil/edit.html.erb`.
  - Verificado con la suite completa (**147 tests, 460 assertions**, sigue en verde), Rubocop y Brakeman limpios, y contra el server real logueado como el cliente de prueba por `curl`: las 4 secciones se ven con línea negra entre ellas, solo "Mis direcciones"/"Mis reseñas" traen `.perfil-seccion-gris`, `/users/edit` trae los 7 campos (nombre/apellido/teléfono/email/contraseña nueva/confirmar/contraseña actual), y guardar un cambio ahí redirige a `/perfil` (no a la Home). **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que las líneas negras, el gris acotado a 2 secciones y los botones grandes se ven como se pidió.
  **Ajuste de alineación (2026-08-25, tras verlo Joaquín)**:
  - **Alto disparejo respecto de las líneas negras**: las 2 secciones grises tenían un padding propio (`.perfil-seccion-gris`, 2rem) *además* del padding de `.perfil-seccion` (2.5rem) que ya las envolvía — quedaban con más aire arriba/abajo que "Mis datos"/"Mis pedidos". Se fusionó todo en un solo elemento: `.perfil-seccion-gris` ahora es un modificador sobre el mismo div de `.perfil-seccion` (ya no hay un div anidado adentro), así las 4 secciones comparten exactamente el mismo padding vertical — mismo alto respecto de las líneas para las 4.
  - **Ancho disparejo**: por el mismo motivo (el padding horizontal extra de la caja gris), el contenido de "Mis direcciones"/"Mis reseñas" arrancaba más adentro que "Mis datos"/"Mis pedidos". Al fusionar los divs, `.perfil-seccion` pasó a tener `padding: 2.5rem 2rem` parejo en las 4 secciones (antes era `2.5rem 0`, sin margen horizontal) — ahora las 4 arrancan y terminan exactamente en el mismo ancho, y el fondo gris (que ahora vive en el mismo elemento con el padding) sigue llegando de línea a línea sin achicarse.
  - **Botones "Editar datos" y "Nueva dirección" ahora miden exactamente lo mismo**: antes cada uno tomaba el ancho de su propio texto + padding fijo, así que salían de distinto tamaño (el texto "Nueva dirección" es más largo). Se les dio un ancho fijo de `260px` (`padding-left`/`padding-right: 0`, ya no dependen del padding para centrarse — `.btn` ya centra el texto) — mismo ancho y alto para los dos, y arrancan en la misma posición horizontal porque ahora viven dentro de secciones con el mismo padding.
  - **Texto de "Mis reseñas" completado**: el nombre del producto (link dentro de `.resena-item-header`) no estaba incluido en el set de selectores que unifica tamaño/color — quedaba con el estilo azul/subrayado por defecto del navegador en vez del cuerpo gris de "Quiénes somos". Se agregó `.resena-item-header a` al mismo grupo de reglas.
  - Verificado con la suite completa (147 tests, 460 assertions, sigue en verde) y contra el server real por `curl` confirmando el HTML/CSS servidos. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que ahora todo alinea (alto, ancho y botones).
- [x] **Ficha de producto: colores de `--ocean` a `--navy` clásico** (2026-08-25, a pedido de Joaquín, punto 6 de la revisión view-por-view). La ficha de producto (`/productos/:id`) usaba el celeste-azulado `--ocean` (#145577) en varios lugares en vez del navy (`--navy`, #081f2e) que usa el resto del sitio para sus acentos principales (títulos, botones primarios). Cambiado a navy en los 4 lugares donde aparecía: el link "← Volver al catálogo" (`.producto-detalle-volver`), el precio (`.producto-detalle-precio`), el botón "Agregar al carrito" (pasó de `btn-ocean` a `btn-primary`) y el color de la estrella llena en las reseñas (`.icon-estrella-llena`, clase compartida por `shared/_estrellas` — el cambio también se ve reflejado en "Mis reseñas" del perfil, que usa el mismo componente). Archivos: `app/assets/stylesheets/application.css`, `app/views/productos/show.html.erb`. Verificado con la suite completa (147 tests, sigue en verde) y contra el server real por `curl` confirmando que el botón ya usa `btn-primary`. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme el cambio de color.
  **Reordenamiento en 3 secciones (2026-08-25, a pedido de Joaquín)**: la ficha pasó a tener una estructura clara de 3 partes.
  - **"← Volver al catálogo" salió de la columna de información** y ahora es un elemento propio arriba de todo, alineado a la izquierda — queda justo encima del borde superior de la foto (antes vivía metido arriba del `<h1>`, dentro de la misma columna de texto).
  - **Foto (izquierda) e información (derecha) en dos columnas iguales** (`grid-template-columns: 1fr 1fr`, ya existía) — sin cambios de proporción, solo se ordenó qué vive en cada una.
  - **Botón "Agregar al carrito" alineado con el borde inferior de la foto**: `.producto-detalle` pasó de `align-items: start` a `stretch`, así la columna de información se estira a la altura completa de la fila (que define la foto, que sigue con su propio `align-self: start` para no deformarse). Dentro de esa columna, `.producto-detalle-info` y el `<form>` pasaron a `display: flex; flex-direction: column` y el botón a `margin-top: auto` — mismo truco que ya usa `.quienes-somos-texto` en la Home para pegar su botón contra el borde inferior de la foto de esa sección.
  - **Reseñas como tercera sección, con título de sección real**: el `<h2>Reseñas</h2>` genérico (1.3rem, sin color) pasó a `.producto-resenas-titulo`, con el mismo formato de título que ya usan las demás secciones rediseñadas hoy (Fraunces 1.8rem/600/navy — mismo patrón que `.perfil-seccion-titulo`/`.checkout-seccion-titulo`). La línea separadora de arriba de esta sección pasó de gris (`var(--border)`) a negra (`#000`), para quedar consistente con el resto de separadores de sección del sitio.
  - Archivos: `app/views/productos/show.html.erb`, `app/assets/stylesheets/application.css`. Verificado con la suite completa (147 tests, 460 assertions, sigue en verde) y contra el server real por `curl` confirmando que el link "Volver al catálogo" ya aparece antes del grid foto/información en el HTML, y que el título de reseñas trae la clase nueva. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que el botón queda a la altura del borde inferior de la foto y que el orden/jerarquía de letras se ve bien.
- [x] **Carrito: botón "Completar datos de envío" de `--ocean` a navy** (2026-08-25, a pedido de Joaquín). Mismo motivo que el cambio de la ficha de producto: usaba `btn-ocean` en vez del navy clásico del resto del sitio. Cambiado a `btn-primary` en `app/views/carrito/_resumen.html.erb`. No se tocó el botón "Ver carrito" del modal de "producto añadido" (`carrito/_modal.html.erb`, sigue en `btn-ocean`) — no fue parte de lo pedido. Verificado con la suite completa (147 tests, sigue en verde) y contra el server real por `curl` (agregando un producto real al carrito) confirmando que el botón ya renderiza con `btn-primary`. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme el color.
- [x] **Checkout: letra más grande y legible** (2026-08-25, a pedido de Joaquín — le gustaron las 2 secciones ya existentes, pero la letra quedaba muy chica). Los labels, inputs/selects/textarea y todo el texto secundario de esta página venían con los tamaños chicos por defecto pensados para formularios cortos (admin, registro) — se agrandaron **solo en el checkout**, sin tocar el resto de los formularios del sitio (via `.checkout-seccion label`/`.checkout-seccion input...` en vez de editar las reglas globales de `label`/`input`/`select`):
  - Labels: 0.9rem → 1.05rem (y color de #555 a #444, un poco más oscuro para más contraste).
  - Campos de texto/select/textarea: 0.95rem → 1.05rem, con un poco más de padding (0.65rem → 0.75rem).
  - Hint del número de depto (`.field-hint`, dentro del checkout): 0.78rem → 0.88rem.
  - Peso del producto y detalle de dirección guardada (ambos en `<small>`, sin tamaño propio antes — heredaban el tamaño chico por defecto del navegador para `<small>`): ahora 0.95rem explícito.
  - Nota de envío gratis: 0.85rem → 0.95rem.
  - Filas del resumen (dirección, fecha, instrucciones): 0.95rem → **1.1rem** — es el bloque más importante para confirmar antes de pagar, el que más se beneficia de quedar bien legible. Como esta clase (`.checkout-resumen-dato`) también la reutiliza la pantalla de "pedido confirmado"/"consultar pedido" (mismo patrón, a propósito), el aumento se ve reflejado ahí también.
  - Aviso de campos obligatorios faltantes: 0.9rem → 1rem.
  - Opciones de "Dirección de entrega" guardada: 0.9rem → 1rem.
  - Verificado con la suite completa (147 tests, 460 assertions, sigue en verde) y contra el server real por `curl` confirmando que `/checkout` sigue renderizando las 2 secciones, el resumen y el selector de direcciones sin errores. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que ahora se lee claro.
- [x] **Checkout: se sacó el aviso de campos faltantes, resumen colapsable, botón ancho completo y 3 subsecciones en "Completa la información"** (2026-08-25, a pedido de Joaquín).
  - **Se sacó el aviso "Antes de confirmar, completa: ..."** (`.checkout-requisitos-aviso`) que aparecía dinámicamente arriba del botón de confirmar. Se eliminó el controller Stimulus que lo alimentaba (`checkout_requisitos_controller.js`, sin otros usos en el sitio), su target en el form (`data-controller="checkout-resumen checkout-requisitos"` → solo `checkout-resumen`) y el CSS asociado. La validación real sigue intacta en el servidor (`Order`) — esto solo sacaba el aviso visual en vivo.
  - **"Resumen de tu pedido" ahora es colapsable**, mismo patrón "v" que ya usa la pantalla de "Pedido confirmado" (`checkout-seccion-toggle` + `colapsable_controller.js`, chevron que rota 180° al abrir/cerrar) — a diferencia de esa pantalla, acá arranca **abierto** por defecto (`aria-expanded="true"`, sin `hidden`), porque es el resumen que hay que revisar antes de pagar, no conviene esconderlo de entrada. El botón "Confirmar pedido" y "Volver al carrito" quedaron **fuera** del contenido colapsable (siempre visibles, no se ocultan si se cierra el resumen).
  - **Botón "Confirmar pedido" a ancho completo**: `.checkout-boton-confirmar` pasó de ancho automático (texto + padding) a `display: block; width: 100%` — ocupa todo el ancho disponible de la sección, igual que ya hacía el botón "Agregar al carrito" de la ficha de producto.
  - **"Completa la información para tu pedido" dividida en 3 subsecciones**, cada una con su propio subtítulo (`.checkout-subseccion-titulo`, nuevo — Fraunces 1.3rem/600/navy, un escalón por debajo del título de sección de 1.8rem, mismo criterio tipográfico): **"Información de contacto"** (nombre, apellido, teléfono, email), **"Dirección de entrega"** (selector de dirección guardada, calle, comuna, número de depto/casa, **y las instrucciones de entrega** — se decidió agruparlas acá y no en la subsección de fecha, porque son datos sobre cómo llegar/acceder a la dirección, no sobre cuándo) y **"Fecha de entrega"** (el calendario de viernes). Ningún campo cambió de nombre/comportamiento, solo se reorganizó visualmente con títulos que antes no existían dentro de esta sección.
  - **Línea separadora entre las 2 grandes secciones pasó de gris (`var(--border)`) a negra (`#000`)** — mismo criterio "clásico" ya aplicado hoy en Perfil, Nosotros y la ficha de producto.
  - Archivos: `app/views/checkout/show.html.erb` (reestructurada), `app/assets/stylesheets/application.css`. Se eliminó `app/javascript/controllers/checkout_requisitos_controller.js`.
  - Verificado con la suite completa (147 tests, 460 assertions, sigue en verde) y contra el server real por `curl`: las 3 subsecciones aparecen con sus títulos, el toggle colapsable (`checkout-seccion-toggle`) está presente, el aviso de requisitos ya no aparece en el HTML, y el botón de confirmar renderiza. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre, sin Chromium en este entorno) — falta que Joaquín confirme que el toggle abre/cierra bien y que las 3 subsecciones se distinguen claramente.
- [x] **Checkout: asterisco rojo en los campos obligatorios, que desaparece al completarlos** (2026-08-25, a pedido de Joaquín — pidió recuperar de alguna forma el aviso de "falta completar" que se acababa de sacar, pero como marca por campo en vez del cartel arriba del botón). Controller Stimulus nuevo y genérico, `app/javascript/controllers/campo_requerido_controller.js` (`campo-requerido`): se engancha a nivel de `.form-group` (no a un input puntual) y busca el campo adentro con `querySelector("input, select, textarea")` — así funciona igual para un texto, un `<select>` o el input oculto del calendario de viernes, sin necesitar un target explícito por tipo de campo. Al conectarse (y en cada `input`/`change`) esconde el `*` si el campo ya tiene un valor. Aplicado a los 7 campos realmente obligatorios: nombre, apellido, teléfono, email, calle, comuna y fecha de entrega — los 2 campos opcionales (número de depto/casa, instrucciones) no llevan asterisco. Funciona también con el autocompletado por JS (elegir una dirección guardada, elegir un viernes en el calendario) porque esos controllers ya disparaban sus propios eventos `input`/`change` con `bubbles: true` — no hizo falta tocarlos. Asterisco en rojo (`#c0392b`, mismo rojo de error que usa el resto del sitio, clase `.campo-requerido-marca`). Verificado con la suite completa (147 tests, sigue en verde), sintaxis del controller JS validada, y contra el server real por `curl` confirmando que los 7 campos obligatorios traen el asterisco y los 2 opcionales no. **No se pudo confirmar visualmente en un browser real** que el asterisco efectivamente desaparezca al escribir (mismo motivo de siempre) — falta que Joaquín lo prueba a mano.
- [x] **Consultar mi pedido: botón "Buscar" reemplazado por la lupa embebida en el campo** (2026-08-25, a pedido de Joaquín). El botón de texto separado ("Buscar", `btn btn-primary`) se sacó — ahora es un ícono de lupa (mismo SVG de línea que ya usa el buscador del catálogo) dentro del propio input de código de pedido, a la derecha, como botón `type="submit"` real (funciona con click o con Enter, no es solo decorativo). CSS nuevo y propio de esta página (`.consulta-pedido-buscar-wrap`/`.consulta-pedido-buscar-btn`) — no se reutilizó directamente el `.filtro-buscar` del catálogo porque ese está pensado para la barra de filtros chica (2.75rem de alto, 0.88rem de letra); acá el input es más grande (3rem), consistente con el resto de esta página. Verificado con la suite completa (147 tests, sigue en verde — incluye los 2 tests de integración existentes que postean a `buscar_pedido_publico_path`, sin cambios de comportamiento) y contra el server real por `curl` confirmando que el botón de texto ya no está y el nuevo botón-lupa sí. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que se ve bien.
- [x] **Registrarse: letra más grande y clara** (2026-08-25, a pedido de Joaquín). Es el formulario de auth más largo del sitio (nombre, apellido, email, teléfono, dirección completa, contraseña) y venía con la letra chica por defecto de FORMULARIOS (misma familia de tamaños que ya se agrandó en el Checkout). Cambio acotado **solo a esta página**, no al resto de pantallas que comparten `.auth-card` (login, recuperar contraseña) — se agregó una clase nueva `auth-card--registro` al wrapper de `devise/registrations/new.html.erb` y se escoparon las reglas a esa clase. Como el formulario de dirección (`direcciones/_fields.html.erb`, con etiqueta/comuna/calle/número de depto) se renderiza anidado dentro de esta misma página vía `f.fields_for :direcciones`, el ajuste de tamaño lo alcanza automáticamente sin tocar ese partial (que se sigue usando sin cambios en `direcciones/new` y `direcciones/edit`, donde no correspondía este ajuste).
  - Labels: 0.9rem → 1.05rem (color de #555 a #444).
  - Campos de texto/email/teléfono/contraseña/select: 0.95rem → 1.05rem, con más padding (0.65rem → 0.75rem).
  - Hint de contraseña mínima y de número de depto/casa (`.field-hint`): 0.78rem → 0.88rem.
  - "¿Ya tienes cuenta? Inicia sesión aquí": 0.9rem → 1rem.
  - Botón "Crear cuenta": 1rem → 1.05rem.
  - Archivos: `app/views/devise/registrations/new.html.erb`, `app/assets/stylesheets/application.css`. Verificado con la suite completa (147 tests, sigue en verde) y contra el server real por `curl` confirmando que `/users/sign_up` renderiza con la clase nueva. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que se lee más claro.
- [x] **Admin: filtro de pedidos por fecha de entrega + limpieza de estilos inline en Pedidos/Productos/Usuarios** (2026-08-25, a pedido de Joaquín — cierra el último punto de la revisión view-por-view).
  - **Filtro nuevo en `/admin/pedidos`**: hasta ahora la única forma de filtrar por fecha era el export a Excel (que descarga un archivo, no filtra lo que se ve en pantalla). Se agregó un segundo formulario arriba de la tabla, mismo calendario de viernes (`shared/_selector_viernes`) que ya usa el export, con botón "Filtrar" y un link "Ver todos" que aparece solo cuando hay un filtro activo. `Admin::PedidosController#index` acepta `params[:fecha]` y filtra `Order.where(fecha_entrega: ...)`; una fecha inválida redirige con error (mismo criterio que ya usaba `#exportar`). El mensaje de "no hay pedidos" cambia según si hay un filtro activo o no. 2 tests de integración nuevos (`test/integration/admin_pedidos_test.rb`): filtra correctamente por fecha, y una fecha inválida redirige.
  - **Limpieza de estilos inline en todo el panel admin** (mismo criterio que ya se aplicó en el resto del sitio — el admin se había quedado atrás en esto). 3 clases nuevas y reutilizables:
    - `.volver-link` (`margin-bottom: 1.5rem`) reemplaza el `style="margin-top: 1.5rem"` que se repetía a mano en el `<h1>`/`.admin-header` de cada pantalla "Volver a X" + título — corregido en `admin/pedidos/show.html.erb`, `admin/products/new.html.erb`, `admin/products/edit.html.erb`, `admin/users/show.html.erb`, y de paso también en `direcciones/new.html.erb`/`direcciones/edit.html.erb` (mismo patrón exacto, fuera del admin pero para no dejar la mitad de los casos sin corregir).
    - `.texto-muted` (color gris, sin cambiar tamaño) reemplaza los `style="color:#666"` sueltos dentro de una línea de texto (ej. "(invitado)" en la lista de pedidos, "(Casa)" en la dirección del detalle de un pedido).
    - `.nota-muted` (color gris + letra más chica + margen) reemplaza los `style="color:#666; font-size:0.85rem; ..."` de párrafos aparte (avisos de "pedido de invitado", "dirección anterior al cambio de datos por pedido", nota del export de Excel).
    - `.admin-card--compacto` (ancho máximo 560px) reemplaza el `style="max-width: 560px; ..."` de la tarjeta "Datos del cliente" en `admin/users/show.html.erb`.
    - `.titulo-separado` (margin-top 2rem) reemplaza el `style="margin-top: 2rem"` del `<h2>Historial de estados</h2>` en el detalle de un pedido.
  - No se tocó la estructura general del panel admin (sigue usando `.admin-header`/`.admin-card`/tablas, no el patrón de secciones grandes tipo Home/Perfil) — el pedido era limpiar la deuda de estilos inline y sumar el filtro, no rediseñar el admin desde cero.
  - Verificado con la suite completa (**149 tests, 468 assertions**, sigue en verde — 2 tests nuevos), Rubocop y Brakeman limpios sobre los archivos tocados, y contra el server real logueado como admin por `curl`: `/admin/pedidos` muestra el filtro nuevo, `/admin/products/new`, `/admin/users` y el detalle de un usuario/pedido renderizan sin errores y con las clases nuevas en vez de `style=` inline (confirmado que ya no queda ningún `style="..."` en `app/views/admin/` ni `app/views/direcciones/`). **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que el filtro funciona bien y que el panel se ve prolijo.
  **Ajuste (2026-08-25, tras verlo Joaquín)**: reorganización del filtro + export y botones al patrón estándar.
  - **Filtro y export a Excel, antes 2 cajas separadas que parecían dos cosas repetidas** (cada una con su propio calendario de viernes) — ahora comparten una sola tarjeta (`.admin-herramientas`) en dos columnas (`.admin-herramienta`), con una línea divisoria vertical entre ambas (horizontal en mobile, `@media (max-width: 700px)`). El label de la segunda columna pasó de "Fecha de despacho" a **"Descargar Excel de despacho"** (más claro qué hace esa columna a simple vista) y su botón de "Descargar Excel de despacho" a solo **"Descargar"** (ya no hace falta repetir el texto que ahora está en el label).
  - **Botones al patrón estándar del resto de Pedidos**: "Filtrar" y "Descargar" pasaron de `btn-ocean`/`btn-primary` en tamaño `btn-sm` a `btn btn-primary` sin `btn-sm` — mismo color (navy) y mismo tamaño "normal" que ya usan el resto de los botones de esta sección (`Confirmar pago recibido`, `Cancelar pedido`, etc., en el detalle de un pedido); antes eran más chicos y uno de ellos (`btn-ocean`) todavía tenía el celeste-azulado que se fue sacando del resto del sitio hoy.
  - CSS: se sacó `.exportar-excel-form` (ya no se usaba en ningún lado) y se reemplazó por `.admin-herramientas`/`.admin-herramienta`.
  - Verificado con la suite completa (149 tests, sigue en verde) y contra el server real logueado como admin por `curl`: los 2 botones renderizan como `btn btn-primary` (sin `btn-ocean` ni `btn-sm` en la página), y los labels/textos nuevos aparecen correctos. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que la tarjeta con la línea divisoria se ve prolija.
  **Segundo ajuste (2026-08-25, tras verlo Joaquín — le gustó la separación en 2 columnas, pero pidió mismo formato en ambas y sacar la nota)**:
  - **Las 2 columnas pasaron de una sola fila (label + calendario + botón, todo en línea) a un formato apilado**: el label arriba ("Filtrar por fecha de entrega" / "Descargar Excel de despacho") y debajo, en su propia fila, el calendario con el botón a la derecha (`.admin-herramienta-controles`, nuevo) — mismo formato en las 2 columnas, sin excepciones.
  - **Se sacó la nota** "Descarga solo los pedidos cuya fecha de entrega es la elegida." — ya no hacía falta, el label de esa columna ("Descargar Excel de despacho") deja bastante claro qué hace.
  - `.admin-herramienta` pasó de `display: flex` en fila a `flex-direction: column` (label + `.admin-herramienta-controles` apilados); la línea divisoria entre columnas no cambió.
  - Verificado con la suite completa (149 tests, sigue en verde) y contra el server real por `curl` confirmando que ambas columnas usan `.admin-herramienta-controles` y que la nota ya no aparece en el HTML. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que las 2 columnas quedaron con el mismo formato.
- [x] **Admin: botón "Nuevo producto" bajado del header, sin el "+"** (2026-08-25, a pedido de Joaquín, mismo criterio que "Nueva dirección" en Mi perfil). El botón salió de `.admin-header` (donde estaba al lado del título "Productos") y ahora es su propia fila (`.acciones`) debajo del título, antes de la tabla. Se sacó el "+" del texto (queda "Nuevo producto"). `.acciones` (clase ya existente, reusada sin cambios de comportamiento) sumó `margin-bottom: 1.5rem` para separarla bien de la tabla que viene después — antes solo tenía `margin-top`, pensada para cuando es lo último de una sección, no lo primero. Verificado con la suite completa (149 tests, sigue en verde) y contra el server real por `curl` confirmando que "Nuevo producto" (sin "+") aparece fuera de `.admin-header`, dentro de `.acciones`. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme la posición.
- [x] **Admin: detalle de usuario rediseñado con el mismo formato de "Mi perfil"** (2026-08-25, a pedido de Joaquín — última mejora de esta ronda). La pantalla de "Ver Usuario" era la más floja del panel admin en cuanto a formato — se reescribió reusando tal cual el sistema visual de "Mi perfil" (mismas clases CSS, sin crear nada nuevo): `.perfil-panel` como wrapper, 3 secciones separadas por línea negra (`.perfil-seccion`) con título grande (`.perfil-seccion-titulo`, Fraunces 1.8rem/600/navy) y el mismo tamaño/color de cuerpo (1.2rem/#555) heredado automáticamente por ser las mismas clases.
  - **Se sacó el título con el nombre del cliente** (`<h1>`) igual que se hizo en "Mi perfil" — el nombre ahora es la primera fila (`dt`/`dd`) de la sección "Datos del cliente", junto con email, teléfono y "Cliente desde".
  - **3 secciones, mapeadas 1 a 1 con las de "Mi perfil"**: "Datos del cliente" (~ Mis datos, sin fondo gris), "Direcciones" (~ Mis direcciones, **con fondo gris**, mismo criterio de qué sección lleva gris que en Mi perfil) y "Pedidos" (~ Mis pedidos, sin fondo gris). No se agregó una sección de reseñas — no era parte de esta vista y no se pidió.
  - **Botón "Eliminar usuario" mucho menos llamativo**: pasó de `btn btn-danger` (botón sólido rojo, vivía arriba en el header) a `btn-link-danger` (sin fondo, texto rojo subrayado, `#c0392b`) — la misma clase que ya usan los links "Eliminar" de una dirección no principal en el resto del sitio. Se movió del header (que ya no existe) a la sección "Datos del cliente", como una acción más.
  - Las direcciones ya no muestran acciones de editar/eliminar (el admin solo las consulta, no las gestiona desde acá — eso ya existía así, no se tocó ese comportamiento) y ahora tienen un estado vacío ("Este cliente todavía no tiene direcciones guardadas") que antes no existía (antes, sin direcciones, la sección quedaba con el título y nada debajo).
  - Archivos: `app/views/admin/users/show.html.erb` (reescrita). No se tocó el controller (`@usuario`/`@pedidos` ya alcanzaban) ni el CSS (se reutilizó el ya existente de "Mi perfil" sin modificarlo).
  - Verificado con la suite completa (149 tests, 468 assertions, sigue en verde) y contra el server real logueado como admin por `curl`, probando dos casos: un cliente con direcciones/pedidos (3 secciones, la de Direcciones con `.perfil-seccion-gris`, botón `btn-link-danger` presente) y un cliente sin direcciones (muestra el estado vacío nuevo). **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que se ve igual de prolijo que "Mi perfil".
- [x] **"Recuperar contraseña" simplificado a WhatsApp + botón de reseteo en el admin** (2026-08-25, a pedido de Joaquín). El sitio no tiene un proveedor de SMTP configurado en producción (pendiente marcado desde hace rato en este README) — el formulario de "Recupera tu contraseña" de Devise (que manda un email con un link) nunca le iba a llegar a un cliente real. Se reemplazó por el mismo patrón que ya usa el resto del sitio para todo lo que requiere intervención humana: pedirlo por WhatsApp.
  - **`devise/passwords/new.html.erb`**: se sacó el formulario de email — ahora es un CTA de WhatsApp (`.whatsapp-cta`, misma caja verde que ya se usa en la confirmación de pedido) con un mensaje pre-escrito ("Hola, olvidé mi contraseña y necesito ayuda para recuperar el acceso a mi cuenta."). El link "¿Olvidaste tu contraseña?" del login sigue apuntando al mismo lugar de siempre (`new_user_password_path`), solo cambió lo que hay ahí.
  - **No se tocó nada del lado de Devise** (el módulo `:recoverable`, las rutas, `PasswordsController`, el mailer y `devise/passwords/edit.html.erb` siguen intactos) — a propósito, por si en algún momento se configura SMTP y se quiere volver a habilitar el flujo por email no hay que reconstruir nada, solo devolver el formulario a esta vista. Los tests existentes que prueban ese flujo por controller directamente (`test/integration/registro_login_test.rb`) siguen pasando sin cambios.
  - **Botón nuevo "Resetear contraseña" en `/admin/users/:id`** (dentro de "Datos del cliente", junto al ahora más discreto "Eliminar usuario"): `Admin::UsersController#resetear_password` (`PATCH /admin/users/:id/resetear_password`, ruta nueva) genera una contraseña aleatoria de 10 caracteres (`SecureRandom.alphanumeric`), se la asigna al cliente, y la muestra en el flash de la página ("Nueva contraseña para pasarle al cliente por WhatsApp: ...") para que el admin la copie y se la pase a mano en la conversación — mismo criterio manual que ya se usa para confirmar pagos, no hay nada automático de por medio.
  - Test nuevo en `test/integration/admin_users_test.rb`: resetea la contraseña, confirma que el flash trae una nueva contraseña válida y que la vieja dejó de funcionar.
  - Verificado con la suite completa (**150 tests, 474 assertions**, sigue en verde — 1 test nuevo), Rubocop y Brakeman limpios, y de punta a punta contra el server real por `curl`: `/users/password/new` ya no tiene formulario (solo el botón de WhatsApp), y el reseteo desde el admin devuelve la contraseña nueva en el flash y efectivamente la deja funcionando (`valid_password?`). **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que la pantalla de recuperar contraseña se ve bien.
- [x] **Buscador en Usuarios y en Pedidos (admin)** (2026-08-25, a pedido de Joaquín).
  - **`/admin/users`**: nuevo campo de búsqueda (mismo componente de lupa embebida que ya usa "Consultar mi pedido", reusado acá como `.admin-buscador`) que filtra por nombre, apellido, nombre completo, email o teléfono. `Admin::UsersController#index` arma un `WHERE ... ILIKE` con parámetros ligados (sin interpolar el texto de búsqueda directo en el SQL, evita inyección — confirmado limpio con Brakeman).
  - **`/admin/pedidos`**: mismo componente de búsqueda, arriba de la caja de "Filtrar por fecha" / "Descargar Excel" (son 2 herramientas independientes — buscar por texto no filtra por fecha ni viceversa, cada una es su propio formulario GET). Busca por código de pedido, nombre/apellido/email de contacto del pedido (`orders.nombre_contacto`, etc. — funciona también para pedidos de invitado, que no tienen cuenta) y, si el pedido es de un cliente registrado, también por el nombre/email de ese usuario.
  - En ambas pantallas, el mensaje de "no hay resultados" cambia según si hay una búsqueda activa, y aparece un link "Ver todos" para volver a la lista completa.
  - 5 tests de integración nuevos (`admin_users_test.rb`: por nombre y por email; `admin_pedidos_test.rb`: por código, por nombre de invitado, por nombre de cliente registrado).
  - Verificado con la suite completa (**155 tests, 499 assertions**, sigue en verde), Rubocop y Brakeman limpios, y contra el server real logueado como admin por `curl`: buscar por el código de un pedido real devuelve exactamente ese pedido. **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre) — falta que Joaquín confirme que los 2 buscadores se ven y funcionan bien.

- [x] **Pedido manual desde el panel admin** (2026-08-31, a pedido de Joaquín). Para cargar ventas que llegaron por fuera del carrito (alguien escribió directo por WhatsApp) sin inventar una cuenta ni pasar por el checkout normal.
  - **Columna nueva `orders.source`** (migración `AddSourceToOrders`, `"web"`/`"manual"`, default `"web"` — los 46 pedidos existentes quedan como `"web"` automáticamente). `Order#manual?` predicado nuevo.
  - **`/admin/pedidos/nuevo`** (botón "Nuevo pedido manual" en el listado): formulario con selector de cliente existente (`<select>` simple con nombre+teléfono — no hay tantos clientes todavía como para justificar un buscador tipo autocompletar; se puede sumar después si la lista crece) que al elegir uno prellena nombre/apellido/teléfono/email/dirección (Stimulus `pedido_cliente_controller.js`, mismo patrón que ya usa `direccion_selector_controller.js` en el checkout) — los campos quedan editables igual, y elegir "Cliente nuevo" los limpia para cargar un pedido de invitado (mismo criterio que el checkout de invitado, sin necesidad de cuenta). Líneas de producto dinámicas (agregar/quitar sin recargar, `pedido_items_controller.js` clona un `<template>` con `fields_for` — mismo truco que usan gemas como cocoon, sin agregar ninguna gema nueva) con un total estimado en vivo (solo ayuda visual; el total real siempre se recalcula en el servidor). Estado inicial (cualquiera de los estados de `Order`) y fecha de entrega (mismo calendario de viernes que ya usa el export/filtro de fecha, sin restricción de mínimo — a diferencia del checkout, acá sí se puede elegir un viernes ya pasado para dejar registrada una venta que ya se despachó).
  - **No hay selector de "medio de pago"**: el sitio entero solo maneja transferencia bancaria (no existe ese campo en ningún lado de `Order`), así que no había nada que agregar ahí.
  - **Precio siempre desde el precio actual del producto** (nunca desde lo que mande el formulario) y **stock se descuenta igual que en el checkout normal** — con una validación nueva (`Order#stock_suficiente_por_item`, activa solo si `manual?`) que rechaza el pedido si se pide más cantidad de la que hay, en vez de dejar el stock en negativo silenciosamente. También se exige al menos un producto (`Order#debe_tener_al_menos_un_producto`, misma condición `if: :manual?`) — el checkout normal no necesita esta validación porque agrega los `order_items` recién después de guardar el pedido, dentro de una transacción.
  - **La regla de "no se puede pedir fecha de entrega anterior al próximo viernes hábil" se desactivó solo para pedidos manuales** (`unless: :manual?` en esa validación) — tiene sentido para un cliente en el checkout, no para el admin registrando una venta que ya pasó.
  - **Badge "Manual"** junto al código en el listado de `/admin/pedidos` y como fila "Origen" en el detalle del pedido — color discreto (`#d6e4ec`/`--ocean`, no un color de alerta) para no competir con el badge de estado, que es el dato principal de esa columna.
  - **Excel de despacho**: no necesitó ningún cambio — el pedido manual es un `Order` real con su `fecha_entrega`, así que ya aparece en el export existente exactamente igual que cualquier otro pedido de esa fecha (confirmado extrayendo el `.xlsx` generado y buscando el código del pedido de prueba adentro).
  - **Archivos**: migración nueva, `app/models/order.rb`, `app/controllers/admin/pedidos_controller.rb`, `config/routes.rb`, `app/views/admin/pedidos/{new,_item_fields}.html.erb` (nuevos), `app/views/admin/pedidos/{index,show}.html.erb`, `app/javascript/controllers/{pedido_cliente,pedido_items}_controller.js` (nuevos), `app/assets/stylesheets/application.css`.
  - Verificado con la suite completa (**168 tests, 529 assertions**, sigue en verde — 13 tests nuevos: 6 de modelo cubriendo las validaciones condicionadas a `manual?` y 7 de integración cubriendo el formulario, la creación con cliente nuevo/existente, fecha pasada, sin productos, stock insuficiente y el badge en el listado), Rubocop y Brakeman limpios, y de punta a punta contra el server real por `curl` con un usuario admin temporal: el formulario carga, un pedido manual real se crea con el precio/stock/total correctos, aparece con el badge en el listado y con "Origen: Manual" en el detalle, un envío sin productos rechaza con el mensaje esperado, y el pedido de prueba aparece dentro del `.xlsx` exportado para su fecha de despacho (usuario/token/pedido de prueba eliminados después de verificar, sin dejar datos falsos en la base de desarrollo). **No se pudo confirmar visualmente en un browser real** (mismo motivo de siempre, sin Chromium en este entorno) — falta que Joaquín pruebe el formulario a mano, en particular el prellenado al elegir un cliente y el agregar/quitar líneas de producto.
